# V11 Research Run R9C — 2026-08-24

Owner-authorized restart of the 94-symbol bounded V11 research round after isolating research workflow concurrency from unrelated `main` pushes.

Invariant contract remains unchanged: exact instrument data only; per-symbol DEV/VALIDATION refinement; RR exactly 1:1 or 1:2; WR >= 80%; positive expectancy; 1-3 real executions per eligible day; no pooling, fabrication, lookahead, silent eligible-day deletion, symbol deletion, or FINAL tuning; Claude + Codex + DeepSeek + Qwen + OpenRouter must all be OK; batched post-review remains required.

This file is an execution trigger marker only and does not modify production Signal V11 behavior.
