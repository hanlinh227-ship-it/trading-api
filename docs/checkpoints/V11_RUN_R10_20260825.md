# V11 Research Run R10 — 2026-08-25

Owner-authorized single restart of the current 94-symbol V11 research loop after the IPUSDT catalog migration and prior bounded refinements already present on current GitHub main.

Hard contract: exactly 94 catalog symbols; exact instrument data only; independent per-symbol DEV/VALIDATION refinement; RR exactly 1:1 or 1:2; WR >= 80.00%; positive expectancy; 1-3 real executions per eligible day; no pooling, fabrication, lookahead/leakage, silent eligible-day deletion, symbol deletion, proxy promotion, or FINAL tuning. Untouched FINAL may run only after all 94 pass VALIDATION and Claude + Codex + DeepSeek + Qwen + OpenRouter are all OK. Batched post-review remains mandatory to bound provider latency.

This checkpoint is an execution trigger marker only. It does not change Signal V11 production behavior, execution authority, quote routing, risk gates, Telegram behavior, TRADING_STATE, or Binance Auto authority.
