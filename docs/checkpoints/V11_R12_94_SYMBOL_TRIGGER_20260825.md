# V11 R12 — 94-SYMBOL CANONICAL DIRECT RESEARCH TRIGGER

Date: 2026-08-25
Scope: research-only orchestration marker.

Verified immediately before trigger on fresh GitHub `main`:
- shared V11 catalog contains 94 symbols;
- `IPUSDT` is absent from the active shared V11 catalog;
- canonical direct workflow preflight/summary/FINAL eligibility use catalog count 94;
- canonical direct workflow uses bounded batched post-review and requires Claude, Codex, DeepSeek, Qwen, and OpenRouter all `OK` for compliant FINAL eligibility;
- `V11_DIRECT_5AI_FINAL_LOCK_20260824.md` identifies `.github/workflows/v11-fiveai-direct-backtest.yml` as the canonical research workflow;
- legacy loop no longer listens to `[V11-RUN]` and now has an isolated concurrency group, removing the proven self-cancellation race that cancelled prior canonical runs;
- Signal V11 production behavior is not changed by this trigger/isolation repair.

This commit triggers exactly one owner-authorized canonical `[V11-RUN]` research execution from the current 94-symbol `main`. Deterministic evidence remains performance truth; untouched FINAL remains sealed until eligibility is truthful.