ENTRY-001 first dispatch reached DeepSeek API but stopped before any source diff because the model response did not match the worker's accepted unified-diff format. AI-INFRA-004 hardened the parser to accept both git-style and standard a/b unified diffs and to use one bounded format-retry instead of failing immediately. No Trading Signal/runtime source changed in the failed attempt or infrastructure hotfix.
