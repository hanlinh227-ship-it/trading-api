import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {BYBIT_AUTO_CONFIG,bybitAutoConfig,bybitExecutionMode} from './bybit-auto-config.js';
import {drawdownState,btcRiskDecision,equityScaleState,capitalBaseState,sizeBtcSetup} from './bybit-btc-risk-engine.js';
import {selectBtcSetup} from './bybit-btc-strategy.js';

const root=process.cwd();
const read=f=>fs.readFileSync(path.join(root,f),'utf8');
const readRepo=f=>fs.readFileSync(path.resolve(root,'..',f),'utf8');
const required=['index.js','bybit-runtime-contract.js','bybit-auto-config.js','bybit-auto-controller.js','bybit-auto-hub.js','bybit-control-plane.js','bybit-readonly-health.js','bybit-v5-client.js','bybit-btc-balance-reconciler.js','bybit-btc-microstructure-client.js','bybit-btc-market-state.js','bybit-btc-strategy.js','bybit-btc-risk-engine.js','bybit-btc-engine.js','prepare-wrangler.mjs','providers/bybit-signed-client.js','providers/telegram-client.js'];
for(const f of required)assert.ok(fs.existsSync(path.join(root,f)),`MISSING ${f}`);
for(const old of ['bybit-auto-v1.js','v11','providers/anthropic-client.js','providers/decision-evidence.js','providers/entry-intelligence.js','providers/indicators.js'])assert.ok(!fs.existsSync(path.join(root,old)),`LEGACY ACTIVE BOT FILE MUST BE REMOVED ${old}`);

const cfg=bybitAutoConfig({});
assert.equal(cfg.symbol,'BTCUSDT');assert.equal(cfg.category,'linear');
assert.equal(cfg.risk.martingale,false);assert.equal(cfg.risk.gridRescue,false);assert.equal(cfg.risk.addToLoser,false);assert.equal(cfg.risk.pyramidWinner,true);
assert.equal(cfg.scan.hardDailyTradeQuota,false);assert.equal(cfg.scan.entryQuotaPerDay,null);assert.equal(cfg.scan.timeGate,false);assert.equal(cfg.scan.sessionGate,false);assert.equal(cfg.scan.cooldownGate,false);
assert.equal(cfg.execution.noTimeGate,true);assert.equal(cfg.execution.managementEveryMarketStateChange,true);assert.equal(cfg.risk.timedPause,false);assert.equal(cfg.risk.lossStreakTimeGate,false);
assert.equal(cfg.risk.fullAccountAuthority,true);assert.ok(cfg.risk.absoluteSingleEntryRiskPct<=1.6);assert.ok(cfg.risk.maxPortfolioMarginPct<=85);assert.ok(cfg.risk.minFreeReservePct>=10);
assert.equal(cfg.leverage.authority,'EQUITY_TAPERED_CLUSTER_LEVERAGE');assert.equal(cfg.leverage.holdConstantInsideOpenCluster,true);
assert.equal(cfg.positionControl.authority,'STRUCTURE_FLOW_STABILITY_EXIT_FRESH_THESIS_REENTRY');assert.equal(cfg.positionControl.instabilityExit,true);assert.equal(cfg.positionControl.freshThesisReentry,true);assert.equal(cfg.positionControl.recoveryMartingale,false);assert.equal(cfg.positionControl.recoveryAddToLoser,false);
assert.equal(cfg.scalp.authority,'ULTRAFAST_TIERED_NET_EDGE_SCALP');assert.ok(cfg.scalp.minPlannedNetProfitUsd>0);assert.ok(cfg.scalp.minPlannedNetProfitPct>0);assert.ok(cfg.scalp.profitLockR<=.40);assert.ok(cfg.scalp.trailStartR<1);assert.equal(cfg.execution.adaptiveOrderRouting,true);assert.equal(cfg.execution.iocLimitForPassiveEdge,true);assert.equal(cfg.execution.nativeTpAlways,true);assert.equal(cfg.execution.postOnlyPreferredForPassive,false);
assert.equal(cfg.features.ultraFastFlow1s3s,true);assert.equal(cfg.features.marketPulseConsensus,true);assert.equal(cfg.features.tieredEntryRisk,true);assert.equal(cfg.entries.probeConfirmFull,true);assert.ok(cfg.risk.tierUpgradeMinR>0&&cfg.risk.tierUpgradeMinR<.5);
const levRows=cfg.leverage.equityAdaptive.steps;assert.ok(levRows.length>=5);assert.ok(levRows[0].normal>levRows.at(-1).normal);assert.ok(levRows[0].strong>levRows.at(-1).strong);assert.ok(levRows[0].aPlus>levRows.at(-1).aPlus);assert.ok(levRows[0].max>levRows.at(-1).max);
assert.equal(bybitExecutionMode({BYBIT_AUTO_LIVE:'true'}),'PAPER');assert.equal(bybitExecutionMode({BYBIT_AUTO_LIVE:'true',BYBIT_BTC_LIVE_ACK:'true'}),'LIVE');

const dd4=drawdownState({equityUsd:96,highWaterUsd:100,cfg}),dd20=drawdownState({equityUsd:80,highWaterUsd:100,cfg});assert.equal(dd4.multiplier,.9);assert.equal(dd20.multiplier,0);assert.equal(dd20.newRiskLocked,true);
const cUp=capitalBaseState({equityUsd:120,walletBalanceUsd:100,cfg});assert.equal(cUp.capitalBaseUsd,105);
const s50=equityScaleState(50,cfg),s60=equityScaleState(60,cfg),s75=equityScaleState(75,cfg);assert.ok(s60.riskMult>s50.riskMult&&s60.riskMult<s75.riskMult);
const setup={side:'Buy',strength:'NORMAL',entryTier:'CONFIRM',riskScale:.82,entry:101,sl:100,tp:103,rr:2,setup:'TEST',regime:'TREND_UP'};
let r=btcRiskDecision({cfg,equityUsd:100,state:{lastWalletBalanceUsd:100,highWaterUsd:100,tranches:[{id:'L1',status:'OPEN',side:'Buy',qty:1,entry:100,sl:99,managedSl:99,initialRiskUsd:1,entryTier:'CONFIRM',createdAt:1}]},setup,markPrice:99});assert.equal(r.ok,false);assert.equal(r.reason,'NO_ADD_TO_LOSER');
const protectedWinner=[{id:'P',status:'OPEN',side:'Buy',qty:1,entry:100,sl:100.1,managedSl:100.1,initialRiskUsd:1,entryTier:'PROBE',createdAt:2}];r=btcRiskDecision({cfg,equityUsd:100,state:{lastWalletBalanceUsd:100,highWaterUsd:100,tranches:protectedWinner},setup,markPrice:101});assert.equal(r.ok,true);assert.equal(r.entryTier,'CONFIRM');
const duplicateProbe=btcRiskDecision({cfg,equityUsd:100,state:{lastWalletBalanceUsd:100,highWaterUsd:100,tranches:[{id:'P2',status:'OPEN',side:'Buy',qty:.001,entry:100,sl:99,managedSl:99,initialRiskUsd:.001,entryTier:'PROBE',createdAt:3}]},setup:{...setup,entryTier:'PROBE',riskScale:.55},markPrice:100.2});assert.equal(duplicateProbe.ok,false);assert.equal(duplicateProbe.reason,'PROBE_ALREADY_ACTIVE_WAIT_CONFIRMATION');

const quant=sizeBtcSetup({setup:{side:'Buy',strength:'NORMAL',entryTier:'PROBE',entry:80930,sl:80850,cost:{totalCostBps:11.2}},riskUsd:.18,maxRiskUsd:.62,filters:{qtyStep:.001,minQty:.001,minNotional:5,maxQty:10},leverage:10,equityUsd:39,capitalBaseUsd:39,marginCapPct:72});assert.equal(quant.ok,true);assert.equal(quant.qty,.002);assert.ok(quant.actualRiskUsd<=quant.hardRiskCapUsd+1e-9);assert.ok(quant.effectiveLossEstimateUsd<=quant.hardRiskCapUsd+1e-9);assert.ok(quant.costReserveUsd>0);
const quantLarger=sizeBtcSetup({setup:{side:'Buy',strength:'STRONG',entryTier:'FULL',entry:80930,sl:80535.5,cost:{totalCostBps:11.2}},riskUsd:1,maxRiskUsd:1.6,filters:{qtyStep:.001,minQty:.001,minNotional:5,maxQty:10},leverage:10,equityUsd:120,capitalBaseUsd:120,marginCapPct:78});assert.equal(quantLarger.ok,true);assert.ok(quantLarger.qty>=.002);assert.ok(quantLarger.actualRiskUsd<=1.6+1e-9);

const noEntry=selectBtcSetup({quality:{freshBook:true,freshTrades:true,spreadOk:true},regime:'HIGH_VOL_SHOCK',trades:{aggressorImbalance:.9},book:{imbalance5:.9},openInterest:{deltaPct:1},direction5:1,direction15:1,direction60:1,efficiency15:.9,crowding:{}});assert.equal(noEntry.ok,false);assert.equal(noEntry.reason,'HIGH_VOL_SHOCK_NO_NEW_RISK');
const momentumEarly=selectBtcSetup({quality:{freshBook:true,freshTrades:true,spreadOk:true,wsFastPath:true,ultraFastReady:true},microstructureSource:'VPS_BYBIT_WS',regime:'SQUEEZE',price:80000,range5:{width:500,lo:79700,hi:80200},range15:{width:700},structure5:{breakUp:false,breakDown:false},structure15:{recentLow:79700,recentHigh:80300},sweep5:{priorLow:79700,priorHigh:80300},trades:{window1s:{imbalance:.45,trades:8,totalNotional:25000},window3s:{imbalance:.43,trades:18,totalNotional:45000},window5s:{imbalance:.42,trades:28,totalNotional:70000},window15s:{imbalance:.48,trades:70,totalNotional:180000},window60s:{imbalance:.21,totalNotional:520000},burst1x:.8,burst3x:.65,burst5x:.5,priceChange1sBps:1.1,priceChange3sBps:2.4},ultraFast:{flow1:.45,flow3:.43,flow5:.42,flow15:.48,flow60:.21,flowAcceleration:.08,pressureScore:.33,impulseScore:.29,speedScore:.38,signPersistence:4},marketPulse:{direction:'UP',score:.34,confidence:.75,agreement:.85,conflict:.15,speed:.38},book:{imbalance2:.14,imbalance5:.12,micropriceEdgeBps:.08},openInterest:{deltaPct:.04},direction5:.08,direction15:.01,direction60:.02,efficiency15:.08,crowding:{},executionCost:{baseRoundTripCostBps:11,fundingWithinExpectedHold:false,fundingRate:0}});assert.equal(momentumEarly.ok,true);assert.equal(momentumEarly.setup.setup,'SQUEEZE_MOMENTUM_EARLY_RELEASE');assert.equal(momentumEarly.setup.entryTier,'CONFIRM');assert.equal(momentumEarly.setup.executionIntent,'URGENT_MARKET');assert.ok(momentumEarly.setup.cost.netTargetBps>=momentumEarly.setup.cost.minNetBps);
const probe=selectBtcSetup({quality:{freshBook:true,freshTrades:true,spreadOk:true,wsFastPath:true,ultraFastReady:true},microstructureSource:'VPS_BYBIT_WS',regime:'SQUEEZE',price:80000,range5:{width:500,lo:79700,hi:80200},range15:{width:700},structure5:{breakUp:false,breakDown:false},structure15:{recentLow:79700,recentHigh:80300},sweep5:{priorLow:79700,priorHigh:80300},trades:{window1s:{imbalance:.24,trades:4,totalNotional:9000},window3s:{imbalance:.16,trades:9,totalNotional:18000},window5s:{imbalance:.14,trades:14,totalNotional:30000},window15s:{imbalance:.07,trades:28,totalNotional:60000},window60s:{imbalance:-.03,totalNotional:300000},burst1x:.7,burst3x:.6,burst5x:.5,priceChange1sBps:.7,priceChange3sBps:1.4},ultraFast:{flow1:.24,flow3:.16,flow5:.14,flow15:.07,flow60:-.03,flowAcceleration:.05,pressureScore:.09,impulseScore:.10,speedScore:.25,signPersistence:3},marketPulse:{direction:'UP',score:.10,confidence:.42,agreement:.72,conflict:.28,speed:.25},book:{imbalance2:.04,imbalance5:.02,micropriceEdgeBps:.02},openInterest:{deltaPct:0},direction5:.01,direction15:0,direction60:0,efficiency15:.05,crowding:{},executionCost:{baseRoundTripCostBps:8,fundingWithinExpectedHold:false,fundingRate:0}});assert.equal(probe.ok,true);assert.equal(probe.setup.entryTier,'PROBE');
const conflictNoEntry=selectBtcSetup({quality:{freshBook:true,freshTrades:true,spreadOk:true,wsFastPath:true},microstructureSource:'VPS_BYBIT_WS',regime:'SQUEEZE',price:80000,range5:{width:500,lo:79700,hi:80200},range15:{width:700},structure5:{breakDown:true},structure15:{recentLow:79700,recentHigh:80300},sweep5:{priorLow:79700,priorHigh:80300},trades:{window5s:{imbalance:-.16},window15s:{imbalance:-.50},window60s:{imbalance:-.32},burst5x:.07},ultraFast:{flowAcceleration:.34,pressureScore:-.12,impulseScore:.07,signPersistence:2},book:{imbalance2:.28,imbalance5:.28,micropriceEdgeBps:.01},openInterest:{deltaPct:.02},direction5:-.19,direction15:0,direction60:-.18,efficiency15:.01,crowding:{},executionCost:{baseRoundTripCostBps:11,fundingWithinExpectedHold:false,fundingRate:0}});assert.equal(conflictNoEntry.ok,false);

const index=read('index.js'),runtime=read('bybit-runtime-contract.js'),controller=read('bybit-auto-controller.js'),control=read('bybit-control-plane.js'),client=read('bybit-v5-client.js'),engine=read('bybit-btc-engine.js'),risk=read('bybit-btc-risk-engine.js'),strategy=read('bybit-btc-strategy.js'),market=read('bybit-btc-market-state.js'),telegram=read('providers/telegram-client.js'),hub=read('bybit-auto-hub.js'),prep=read('prepare-wrangler.mjs'),workflow=readRepo('.github/workflows/deploy-cloudflare-worker.yml'),bridge=readRepo('bybit-live-bridge/bybit_live_bridge.py');
assert.ok(index.includes('legacyBotsDisabled:true'));assert.ok(index.includes('scheduledExecution:false'));assert.ok(!index.includes('scheduled(event'));assert.ok(!index.includes('recordBybitAutoSchedulerError'));
for(const x of ['BYBIT_BTC_RUNTIME_CONTRACT_V12_ULTRAFAST_TIERED_SCALP','decisionAuthority:\'VPS_WS_MARKET_STATE_CHANGE\'','entryTriggerAuthority:\'VPS_BRIDGE_SECRET_ONLY\'','cronRole:\'NONE_EVENT_DRIVER_ONLY\'','scheduledExecution:false','timeGate:false','strategyCooldown:\'NONE\'','dailyTradeQuota:\'NONE\'','microstructureWindows:\'1S_3S_5S_15S_60S\'','entryTierAuthority:\'PROBE_CONFIRM_FULL\'','EQUITY_TAPERED_CLUSTER_LEVERAGE','ULTRAFAST_TIERED_NET_EDGE_SCALP','plannedNetProfitFloor:true','nativeTpAlways:true','runtimeSwitchDeploymentPolicy:\'PRESERVE_EXISTING\''])assert.ok(runtime.includes(x),`RUNTIME missing ${x}`);
for(const x of ['notifyPendingLiveEntries','ENTRY_ALERTS_CONFIRMED','IDEMPOTENT_WRITE_NO_CHANGE','entrySpacingSec:0','timeGate:false','eventDriven:true','plannedNetProfitUsd','entryTier','THĂM DÒ','XÁC NHẬN'])assert.ok(controller.includes(x),`CONTROLLER missing ${x}`);assert.ok(!controller.includes('cooldownSec'));assert.ok(!controller.includes('BTC_ENTRY_SPACING_'));
for(const x of ['BTC_ENTRY_TRIGGER_REQUIRES_VPS_WS_DRIVER','VPS_BRIDGE_SECRET_ONLY','a.source!=="VPS_BRIDGE_SECRET"','BTC_ENGINE_DISABLED'])assert.ok(control.includes(x),`CONTROL missing ${x}`);
for(const x of ['TRADING_STOP_UNCHANGED','LEVERAGE_UNCHANGED','BYBIT_VPS_BRIDGE_SECRET','unchangedWrite'])assert.ok(client.includes(x),`CLIENT missing ${x}`);
for(const x of ['reconcileTranchesToPosition','reconcilePositionMarginState','EXCHANGE_POSITION_RECONCILED','candidateActualRiskUsd:sized.actualRiskUsd','UNPROTECTED_POSITION_EMERGENCY_FLAT','rewardToTarget','positionStability','MARKET_INSTABILITY_HARD_EXIT','STRUCTURE_FLOW_INSTABILITY_EXIT','FRESH_THESIS_ONLY_NO_RECOVERY_MARTINGALE','EQUITY_TAPERED_CLUSTER_LEVERAGE','PLANNED_NET_PROFIT_BELOW_SCALP_FLOOR','IOC_CAPPED','SCALP_PROFIT_LOCK','nativeTpAlways','entryTier','BYBIT-BTC-HYPERSCALE-2.9-ULTRAFAST-TIERED'])assert.ok(engine.includes(x),`ENGINE missing ${x}`);
for(const x of ['BTC_RISK_RECYCLE_V7_TIERED_SMART_QUANTIZED_SIZING','PROBE_ALREADY_ACTIVE_WAIT_CONFIRMATION','TIER_UPGRADE_WAIT_MARKET_CONFIRMATION','tierRiskFactor','TIER_AWARE_NEAREST_TARGET_WITH_HARD_RISK_AND_MARGIN_CAP'])assert.ok(risk.includes(x),`RISK missing ${x}`);
for(const x of ['BTC_STATE_FIRST_MICROSTRUCTURE_V8_PROBE_CONFIRM_FULL','BREAKOUT_MOMENTUM_PROBE','TREND_MOMENTUM_PROBE','SQUEEZE_MOMENTUM_PROBE','TRANSITION_MOMENTUM_PROBE','entryTier','riskScale','window1s','window3s','VPS_BYBIT_WS'])assert.ok(strategy.includes(x),`STRATEGY missing ${x}`);
for(const x of ['BTC_MARKET_STATE_V5_ULTRAFAST_1S3S_PULSE','marketPulse','window1s','window3s','price=bridge.fresh','fetchBtcMicrostructure'])assert.ok(market.includes(x),`MARKET missing ${x}`);
for(const x of ['window1s','window3s','burst1x','burst3x','priceChange1sBps','priceChange3sBps','CURL_HTTP'])assert.ok(bridge.includes(x),`BRIDGE missing ${x}`);
for(const x of ['TELEGRAM_API_FAILED','TELEGRAM_TRANSPORT_FAILED','j?.ok!==true','AbortSignal.timeout(10000)'])assert.ok(telegram.includes(x),`TELEGRAM missing ${x}`);
for(const x of ['CURRENT_RUNTIME_ENV','retry-until-confirmed','plannedNetProfitUsd','lastPositionReconcile','TP ${px(x.tp)}','SL ${px(x.sl)}','btcPrice','VPS_BYBIT_WS','BTC ${px(s.btcPrice)}'])assert.ok(hub.includes(x),`HUB missing ${x}`);
assert.ok(prep.includes('CRON=NONE_EVENT_DRIVER_ONLY'));assert.ok(!prep.includes("triggers:{crons"));assert.ok(prep.includes('keep_vars:true'));assert.ok(prep.includes('RUNTIME_SWITCHES=PRESERVE_EXISTING'));assert.ok(prep.includes('LIVE_ACK=PRESERVE_EXISTING'));for(const forbidden of ['BYBIT_AUTO_ENABLED','BYBIT_AUTO_LIVE','BYBIT_BTC_LIVE_ACK','BYBIT_AUTO_DEMO','BYBIT_ALLOW_DIRECT_PUBLIC_FALLBACK','BYBIT_ALLOW_DIRECT_PRIVATE_FALLBACK'])assert.ok(!prep.includes(forbidden),`PREP must not overwrite runtime switch ${forbidden}`);
assert.ok(workflow.includes('validate-only'));assert.ok(workflow.includes('BTC_GITHUB_CI_VALIDATION_ONLY=PASS'));assert.ok(!workflow.includes('npx wrangler deploy'));assert.ok(!workflow.includes("BYBIT_BTC_LIVE_ACK: 'false'"));
for(const oldImport of ['forex-','meme-','binance-','hub-v10','hub-v11','hub-v77','hyro-','signal-v10','multi-ai-control-plane','gpt-5ai-action','bybit-btc-execution-policy','bybit-btc-market-model'])assert.ok(!new RegExp(`from\\s+["'][^"']*${oldImport.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}`,'i').test([index,controller,control,engine,strategy,market].join('\n')),`ACTIVE RUNTIME IMPORT CONFLICT ${oldImport}`);

console.log('BTC_STATEFLOW_VALIDATION=PASS');
console.log(JSON.stringify({version:'BYBIT-BTC-STATEFLOW-2.5',symbol:cfg.symbol,autonomous:true,eventDriven:true,timeGate:false,scheduledExecution:false,entryTriggerAuthority:'VPS_BRIDGE_SECRET_ONLY',singleWorkerDeployAuthority:'CLOUDFLARE_BUILDS',runtimeSwitchesPreservedAcrossDeploy:true,ultraFast1s3s:true,marketPulseConsensus:true,probeConfirmFull:true,tierAwareRisk:true,equityTaperedLeverage:true,clusterLeverageLock:true,plannedNetProfitFloor:true,adaptiveOrderRouting:true,nativeTpAlways:true,costAwareProfitLock:true,liveBtcPriceInHub:true,tpSlUsdEntryAlerts:true,instabilityExit:true,freshThesisReentry:true,smartQuantizedSizing:true,exchangePositionReconciliation:true,telegramDeliveryConfirmedOnly:true,telegramEntryRetry:true,idempotentTradingStop:true,noMartingale:true,noAddToLoser:true,winnerPyramiding:true},null,2));
