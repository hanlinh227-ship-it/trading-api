import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {BYBIT_AUTO_CONFIG,bybitAutoConfig,bybitExecutionMode} from './bybit-auto-config.js';
import {drawdownState,btcRiskDecision,activeRiskUsd,equityScaleState} from './bybit-btc-risk-engine.js';
import {selectBtcSetup} from './bybit-btc-strategy.js';

const root=process.cwd();const read=f=>fs.readFileSync(path.join(root,f),'utf8');
const required=['index.js','bybit-runtime-contract.js','bybit-auto-config.js','bybit-auto-controller.js','bybit-auto-hub.js','bybit-control-plane.js','bybit-readonly-health.js','bybit-v5-client.js','bybit-btc-balance-reconciler.js','bybit-btc-microstructure-client.js','bybit-btc-market-state.js','bybit-btc-strategy.js','bybit-btc-risk-engine.js','bybit-btc-engine.js','providers/bybit-signed-client.js','providers/telegram-client.js'];
for(const f of required)assert.ok(fs.existsSync(path.join(root,f)),`MISSING ${f}`);
for(const old of ['bybit-auto-v1.js','v11','providers/anthropic-client.js','providers/decision-evidence.js','providers/entry-intelligence.js','providers/indicators.js'])assert.ok(!fs.existsSync(path.join(root,old)),`LEGACY BOT FILE MUST BE REMOVED ${old}`);

const cfg=bybitAutoConfig({});
assert.equal(cfg.symbol,'BTCUSDT');assert.equal(cfg.category,'linear');assert.equal(cfg.risk.martingale,false);assert.equal(cfg.risk.gridRescue,false);assert.equal(cfg.risk.addToLoser,false);assert.equal(cfg.risk.pyramidWinner,true);assert.equal(cfg.risk.dailyTarget,false);assert.equal(cfg.scan.hardDailyTradeQuota,false);assert.equal(cfg.scan.entryQuotaPerDay,null);assert.equal(cfg.features.stateFirst,true);assert.equal(cfg.features.indicatorsSupportingOnly,true);assert.equal(cfg.features.liquidationFlow,true);assert.ok(cfg.maxTradesPerDay>=1_000_000_000);assert.ok(cfg.risk.maxActiveRiskPct<=10);assert.ok(cfg.risk.absoluteSingleEntryRiskPct<=1.6);assert.ok(cfg.risk.maxPortfolioMarginPct<=85);assert.ok(cfg.risk.minFreeReservePct>=10);assert.equal(cfg.risk.fullAccountAuthority,true);
assert.equal(bybitExecutionMode({BYBIT_AUTO_LIVE:'true'}),'PAPER');assert.equal(bybitExecutionMode({BYBIT_AUTO_LIVE:'true',BYBIT_BTC_LIVE_ACK:'true'}),'LIVE');
const dd4=drawdownState({equityUsd:96,highWaterUsd:100,cfg}),dd7=drawdownState({equityUsd:93,highWaterUsd:100,cfg}),dd10=drawdownState({equityUsd:90,highWaterUsd:100,cfg}),dd15=drawdownState({equityUsd:85,highWaterUsd:100,cfg}),dd20=drawdownState({equityUsd:80,highWaterUsd:100,cfg});assert.equal(dd4.multiplier,.9);assert.equal(dd7.multiplier,.72);assert.equal(dd10.multiplier,.52);assert.equal(dd15.multiplier,.28);assert.equal(dd20.multiplier,0);assert.equal(dd20.newRiskLocked,true);
const s39=equityScaleState(39,cfg),s100=equityScaleState(100,cfg),s500=equityScaleState(500,cfg);assert.equal(s39.riskMult,1);assert.ok(s100.riskMult>1);assert.ok(s500.riskMult>=s100.riskMult);assert.ok(s500.marginCapPct<=84);
const setup={side:'Buy',strength:'NORMAL',entry:101,sl:100,tp:103,rr:2,setup:'TEST',regime:'TREND_UP'};
let r=btcRiskDecision({cfg,equityUsd:100,state:{highWaterUsd:100,tranches:[{id:'L1',status:'OPEN',side:'Buy',qty:1,entry:100,sl:99,managedSl:99,initialRiskUsd:1,createdAt:1}]},setup,markPrice:99});assert.equal(r.ok,false);assert.equal(r.reason,'NO_ADD_TO_LOSER');
r=btcRiskDecision({cfg,equityUsd:100,state:{highWaterUsd:125,tranches:[]},setup,markPrice:101});assert.equal(r.ok,false);assert.equal(r.reason,'DRAWDOWN_NEW_RISK_LOCK');
const riskHeavy=[{id:'OLD',status:'OPEN',side:'Buy',qty:1,entry:100,sl:92,managedSl:92,initialRiskUsd:8,createdAt:1},{id:'NEW',status:'OPEN',side:'Buy',qty:1,entry:100,sl:100.1,managedSl:100.1,initialRiskUsd:1,createdAt:2}];assert.ok(activeRiskUsd(riskHeavy)>=8-1e-9);r=btcRiskDecision({cfg,equityUsd:100,state:{highWaterUsd:100,tranches:riskHeavy},setup,markPrice:101});assert.equal(r.ok,false);assert.equal(r.reason,'ACTIVE_RISK_BUDGET_EXHAUSTED');
const protectedWinner=[{id:'P',status:'OPEN',side:'Buy',qty:1,entry:100,sl:100.1,managedSl:100.1,initialRiskUsd:1,createdAt:2}];r=btcRiskDecision({cfg,equityUsd:100,state:{highWaterUsd:100,tranches:protectedWinner},setup,markPrice:101});assert.equal(r.ok,true);assert.equal(r.fullAccountAuthority,true);
const noEntry=selectBtcSetup({quality:{freshBook:true,freshTrades:true,spreadOk:true},regime:'HIGH_VOL_SHOCK',trades:{aggressorImbalance:.9},book:{imbalance5:.9},openInterest:{deltaPct:1},direction5:1,direction15:1,direction60:1,efficiency15:.9,crowding:{}});assert.equal(noEntry.ok,false);assert.equal(noEntry.reason,'HIGH_VOL_SHOCK_NO_NEW_RISK');

const index=read('index.js');assert.ok(index.includes('runBybitAutoControlled'));assert.ok(index.includes('BTCUSDT'));assert.ok(index.includes('legacyBotsDisabled:true'));
for(const oldImport of ['forex-','meme-','binance-','hub-v10','hub-v11','hub-v77','hyro-','signal-v10','multi-ai-control-plane','gpt-5ai-action'])assert.ok(!new RegExp(`from\\s+["'][^"']*${oldImport.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}`,'i').test(index),`OLD RUNTIME IMPORT ${oldImport}`);
const runtime=read('bybit-runtime-contract.js');for(const x of ['BYBIT_BTC_ONLY','LEGACY_BYBIT_MULTI_COIN_DISABLED=true','LEGACY_FOREX_DISABLED=true','LEGACY_MEME_DISABLED=true','LEGACY_AI_COUNCIL_DISABLED=true','BTC_ONLY_COMPLETE','dailyTradeQuota:\'NONE\'','balanceAuthority:\'BYBIT_WALLET_PLUS_TRANSACTION_LOG\'','depositWithdrawalAware:true'])assert.ok(runtime.includes(x),`RUNTIME missing ${x}`);
const controller=read('bybit-auto-controller.js');for(const x of ['reconcileBtcAccountBalance','balanceAuthority:"BYBIT_WALLET_PLUS_TRANSACTION_LOG"','depositWithdrawalAware:true','netExternalCashFlowUsd'])assert.ok(controller.includes(x),`CONTROLLER missing ${x}`);
const balance=read('bybit-btc-balance-reconciler.js');for(const x of ['/v5/account/transaction-log','TRANSFER_IN','TRANSFER_OUT','lastWalletBalanceUsd','cumulativeExternalCashFlowUsd','highWaterUsd'])assert.ok(balance.includes(x),`BALANCE RECONCILER missing ${x}`);
const engine=read('bybit-btc-engine.js');for(const x of ['BTCUSDT','BTC_LIVE_ACK_REQUIRED_PAPER_SIGNAL','BTC_NATIVE_STOP_VERIFICATION_FAILED_EMERGENCY_FLAT','reduceOnly:true','STRUCTURE_FLOW_INVALIDATION'])assert.ok(engine.includes(x),`ENGINE missing ${x}`);
const strategy=read('bybit-btc-strategy.js');for(const x of ['BREAKOUT_RETEST_FLOW_CONFIRM','TREND_PULLBACK_LIQUIDITY_RECLAIM','RANGE_SELLSIDE_SWEEP_ABSORPTION','LIQUIDATION_EXHAUSTION_RECLAIM','NO_STATE_FIRST_NON_INDICATOR_EDGE'])assert.ok(strategy.includes(x),`STRATEGY missing ${x}`);
const market=read('bybit-btc-market-state.js');for(const x of ['fetchBtcMicrostructure','/v5/market/orderbook','/v5/market/recent-trade','/v5/market/open-interest','/v5/market/account-ratio','micropriceEdgeBps','imbalance2','liquidations'])assert.ok(market.includes(x),`MARKET STATE missing ${x}`);
console.log('BTC_STATEFLOW_VALIDATION=PASS');
console.log(JSON.stringify({symbol:cfg.symbol,strategyAuthority:BYBIT_AUTO_CONFIG.strategyAuthority,nonIndicatorStateFirst:true,liveApiPreserved:true,fullAccountAuthority:true,dailyTradeQuota:'NONE',depositWithdrawalAware:true,balanceAuthority:'BYBIT_WALLET_PLUS_TRANSACTION_LOG',singleRiskMaxPct:cfg.risk.absoluteSingleEntryRiskPct,activeRiskPct:cfg.risk.maxActiveRiskPct,temporaryAPlusRiskPct:cfg.risk.temporaryAPlusActiveRiskPct,marginCapPct:cfg.risk.maxPortfolioMarginPct,noMartingale:true,noAddToLoser:true,winnerPyramiding:true},null,2));
