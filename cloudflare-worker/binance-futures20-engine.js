import {binance20Config,BINANCE20_VERSION} from "./binance-futures20-config.js";
import {binanceUsdm,symbolFilters,floorStep} from "./binance-usdm-client.js";
const avg=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:0,last=a=>a[a.length-1],clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
function ema(a,n){if(!a.length)return 0;const k=2/(n+1);let e=a[0];for(let i=1;i<a.length;i++)e=a[i]*k+e*(1-k);return e;}
function atr(r,n=14){if(r.length<n+1)return 0;const t=[];for(let i=r.length-n;i<r.length;i++){const p=r[i-1][4];t.push(Math.max(r[i][2]-r[i][3],Math.abs(r[i][2]-p),Math.abs(r[i][3]-p)));}return avg(t);}
function parseK(rows){return rows.map(x=>[Number(x[0]),Number(x[1]),Number(x[2]),Number(x[3]),Number(x[4]),Number(x[5])]).filter(x=>x.slice(1).every(Number.isFinite));}
function analyze(symbol,r1,r5,bid,ask,cfg){const c1=r1.map(x=>x[4]),c5=r5.map(x=>x[4]),p=(bid+ask)/2,a1=atr(r1),a5=atr(r5),e9=ema(c1,9),e21=ema(c1,21),e20_5=ema(c5,20),e50_5=ema(c5,50),spreadBps=(ask-bid)/p*10000,atrPct=a1/p*100;if(!(a1>0)||atrPct<cfg.filters.minAtrPct||atrPct>cfg.filters.maxAtrPct||spreadBps>cfg.filters.maxSpreadBps)return null;const c=last(r1),pr=r1[r1.length-2],trend=e20_5>e50_5?1:e20_5<e50_5?-1:0,impUp=c[4]>c[1]&&c[4]>pr[2],impDn=c[4]<c[1]&&c[4]<pr[3],near=Math.min(Math.abs(p-e9),Math.abs(p-e21))<=a1*.55;let side=null,strategy=null,score=0;if(trend>0&&near&&impUp){side="BUY";strategy="M1_TREND_CONTINUATION";score=78;}else if(trend<0&&near&&impDn){side="SELL";strategy="M1_TREND_CONTINUATION";score=78;}else{const look=r1.slice(-26,-3),hi=Math.max(...look.map(x=>x[2])),lo=Math.min(...look.map(x=>x[3]));if(c[4]>hi&&c[4]-hi<=a1*cfg.filters.maxChaseAtr){side="BUY";strategy="M1_BREAKOUT";score=74;}else if(c[4]<lo&&lo-c[4]<=a1*cfg.filters.maxChaseAtr){side="SELL";strategy="M1_BREAKOUT";score=74;}}
if(!side)return null;score+=clamp(Math.abs(e20_5-e50_5)/Math.max(a5,1e-9)*8,0,10);if(score<cfg.filters.minScore)return null;const entry=side==="BUY"?ask:bid,sl=side==="BUY"?Math.min(c[3],e21-a1*.15):Math.max(c[2],e21+a1*.15),risk=Math.abs(entry-sl);if(!(risk>0))return null;const tp=side==="BUY"?entry+risk*cfg.risk.targetRR:entry-risk*cfg.risk.targetRR;return {symbol,side,strategy,score:Math.round(score),entry,sl,tp,rr:cfg.risk.targetRR,atr1:a1,spreadBps};}
export async function scanBinance20(env){const cfg=binance20Config(env),api=binanceUsdm(env),info=await api.exchangeInfo(),out=[];for(const symbol of cfg.symbols){const f=symbolFilters(info,symbol);if(!f||f.status!=="TRADING"||f.contractType!=="PERPETUAL")continue;try{const [k1,k5,b]=await Promise.all([api.klines(symbol,"1m",120),api.klines(symbol,"5m",120),api.bookTicker(symbol)]),setup=analyze(symbol,parseK(k1),parseK(k5),Number(b.bidPrice),Number(b.askPrice),cfg);if(setup)out.push({...setup,filters:f});}catch{}}
out.sort((a,b)=>b.score-a.score);return {version:BINANCE20_VERSION,capitalUsd:cfg.startingCapitalUsd,best:out[0]||null,candidates:out,scannedAt:Date.now()};}
export function sizeBinance20(setup,filters,cfg,equityUsd=20){const riskUsd=Math.min(cfg.risk.perTradeUsd,Math.max(.05,equityUsd*.01)),dist=Math.abs(setup.entry-setup.sl),raw=riskUsd/dist,step=filters.marketStepSize||filters.stepSize,qty=floorStep(raw,step),notional=qty*setup.entry,minNotional=Math.max(5,filters.minNotional||5);if(!(qty>=filters.minQty)||notional<minNotional)return {ok:false,reason:"MIN_NOTIONAL_OR_QTY",qty,notional,minNotional};return {ok:true,qty,notional,riskUsd,leverage:cfg.leverage};}
