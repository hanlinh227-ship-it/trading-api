{
  "name": "bybit-btc-hyperscale",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node validate-worker.mjs && node validate-btc-hyperscale.mjs && node test-skill-gateway.mjs && node test-skill-gateway-handler.mjs",
    "prepare:wrangler": "node prepare-wrangler.mjs",
    "deploy": "npm run prepare:wrangler && npm run check && wrangler deploy --dry-run",
    "preview": "npm run prepare:wrangler && npm run check && wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  }
}
