{
  "name": "trading-v77-scanner",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js",
    "prepare:wrangler": "node prepare-wrangler.mjs",
    "deploy": "npm run prepare:wrangler && wrangler deploy",
    "preview": "npm run prepare:wrangler && wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
