{
  "name": "trading-signal-only-v10",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node validate-worker.mjs",
    "prepare:wrangler": "node prepare-wrangler.mjs",
    "deploy": "npm run check && npm run prepare:wrangler && wrangler deploy",
    "preview": "npm run check && npm run prepare:wrangler && wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.124.0"
  }
}
