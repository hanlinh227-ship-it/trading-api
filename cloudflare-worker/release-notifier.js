const KEY="v771818:release:last_announced";
async function kg(env,key,fb=null){try{return await env.TRADING_STATE?.get(key,"json")??fb;}catch{return fb;}}
async function kp(env,key,v){try{if(env.TRADING_STATE)await env.TRADING_STATE.put(key,JSON.stringify(v));}catch{}}
async function tg(env,text){if(!env.TELEGRAM_BOT_TOKEN||!env.TELEGRAM_CHAT_ID)return false;try{const reply_markup={inline_keyboard:[[{text:"🏠 Mở Hub mới",callback_data:"menu"},{text:"🏦 Hyro",callback_data:"prop:dashboard"}],[{text:"🧠 AI",callback_data:"claude:status"},{text:"🩺 Hệ thống",callback_data:"status"}]]};const r=await fetch(`https://api.telegram.org/bot${env.TELEGRAM_BOT_TOKEN}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:env.TELEGRAM_CHAT_ID,text,reply_markup,disable_web_page_preview:true})}),p=await r.json().catch(()=>null);return !!p?.ok;}catch{return false;}}
export async function announceReleaseOnce(env,{version,name,details=[]}={}){if(!version)return {ok:false,reason:"VERSION_MISSING"};const old=await kg(env,KEY,null);if(old?.version===version)return {ok:true,skipped:true,version};const text=[`🆕 BOT ĐÃ CẬP NHẬT`,`◈ ${version} • ${name||"Trading Runtime"}`,`Mở Hub để kiểm tra giao diện đang active.`,...(details||[]).slice(0,3).map(x=>`• ${x}`)].join("\n");const sent=await tg(env,text);if(sent)await kp(env,KEY,{version,name,announcedAt:Date.now()});return {ok:sent,version,name};}
export async function getReleaseState(env){return kg(env,KEY,null);}
