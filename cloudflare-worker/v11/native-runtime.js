import baseEngine from '../engine-v77168.js';
import {telegramApiRequest} from '../providers/telegram-client.js';
import {V11_CONFIG,V11_GROUPS} from './config.js';
import {buildEntryPlan} from './entry-plan.js';
import {evaluateMarketCandidate} from './market-policies.js';
import {recordV11,refreshV11Lifecycle} from './store.js';
import {getLiveQuote} from './live-quote.js';
import {getSymbolScalpPolicy} from './symbol-scalp-policy.js';
import {evaluateEntryEligibility,evaluateHoldStatus} from './entry-eligibility.js';
const SKEY='v11:native:schedule';
const ACTIONABLE=new Set(['MARKET_SIGNAL','MARKET','LIMIT','MARKET_PLAN','LIMIT_PLAN']);
const PROVIDERS=['claude','codex','deepseek','qwen','openrouter'];
const AI_RECHECK_MS=180000;
async function engine(path,env){const r=await baseEngine.fetch(new Request(`https://v11.internal${path}`),env);try{return await r.json()}catch{return null}}
const finite=v=>{const x=Number(v);return Number.isFinite(x)?x:null};
const px=v=>Number.isFinite(Number(v))?Number(v).toPrecision(7):'—';
const pnlPct=(side,entry,price)=>{entry=Number(entry);price=Number(price);if(!(entry>0)||!(price>0))return null;return (String(side).toUpperCase()==='SHORT'?(entry-price):(price-entry))/entry*100;};
async function notify(env,text){if(!env.TELEGRAM_BOT_TOKEN||!env.TELEGRAM_CHAT_ID)return false;try{await telegramApiRequest(env,'sendMessage',{chat_id:env.TELEGRAM_CHAT_ID,text,disable_web_page_preview:true});return true}catch(e){console.error('V11_TELEGRAM_NOTIFY',e);return false}}
function signalText(x){const c=x.candidate||{},q=c.quote||{},quality=finite(c.qualityScore??c.score),fresh=Number.isFinite(Number(q.quoteAgeSec))?`${Number(q.quoteAgeSec)}s`:'—';return [`⚡ ${String(x.market||'').toUpperCase()} • ${c.side==='LONG'?'BUY':'SELL'} ${x.symbol}`,`E ${px(c.entry)} • SL ${px(c.sl)} • TP ${px(c.tp)}`,`Q ${quality===null?'—':quality.toFixed(0)}/100 • ${c.setup||'—'}${c.promotionMode&&c.promotionMode!=='DIRECT_MARKET'?` • ${c.promotionMode}`:''}`,`📡 ${q.source||'LIVE'} ${fresh}`].join('\n');}
function lifecycleText(x){const c=x.candidate||{},p=pnlPct(c.side,c.entry,x.closePrice),ps=p==null?'—':`${p>=0?'+':''}${p.toFixed(2)}%`,icon=x.outcome==='WIN'?'✅':x.outcome==='LOSS'?'🛑':x.outcome==='CUT'?'✂️':'⌛';return [`${icon} ${x.symbol} • ${x.outcome}`,`E ${px(c.entry)} → ${px(x.closePrice)} • ${ps}`,x.outcome==='CUT'?`AI CUT ${x.cutAiVotes??'—'}/5 • ${x.cutReason||'REEVALUATED'}`:`SL ${px(c.sl)} • TP ${px(c.tp)}`].join('\n');}
function normalizeCandidate(a,q){const planned=a?.planned||{},canonical=a?.canonical||{},mg=canonical.marketGeometry||{},lg=canonical.limitGeometry||{},m15=canonical.m15Location||{},m5=canonical.m5Trigger||{};const atrCandidates=[a?.atr,a?.atr14,a?.indicators?.atr,a?.indicators?.atr14,a?.M5?.atr14,a?.M15?.atr14,a?.H1?.atr14,a?.timeframes?.M5?.atr14,a?.timeframes?.M15?.atr14,a?.timeframes?.H1?.atr14,planned.atr,planned.atr14,mg.atr,lg.atr].map(finite).filter(x=>x>0);const plannedEntry=finite(a?.entry??planned.entry),entry=plannedEntry??finite(q?.price);const qualityScore=finite(a?.qualityScore??a?.quality??a?.score);const structureLevels=[a?.swingLow,a?.swingHigh,a?.support,a?.resistance,a?.structureInvalidation,a?.swingInvalidation,m15.level,m5.level,mg.level,lg.level,planned.sl].map(finite).filter(Number.isFinite);const liquidityLevels=[a?.liquidityLow,a?.liquidityHigh,a?.target1,a?.target2,a?.tp,a?.tp1,a?.tp2,planned.tp,planned.tp1,planned.tp2].map(finite).filter(Number.isFinite);return {...a,price:finite(q?.price),entry,plannedEntry,quote:q,atr:atrCandidates[0]??null,qualityScore,structureLevels,liquidityLevels};}
async function freshAnalyze(symbol,market,env){const [a,q]=await Promise.all([engine(`/analyze?symbol=${encodeURIComponent(symbol)}`,env),getLiveQuote(symbol,market,env)]);if(!a)throw new Error('V11_ANALYSIS_UNAVAILABLE');if(!(Number(q?.price)>0)||q?.fresh!==true)throw new Error('V11_QUOTE_NOT_VERIFIED_FRESH');return {analysis:a,quote:{...q,price:Number(q.price),fresh:true}};}
async function resolveFreshAnalysis(raw,market,env){const q=await getLiveQuote(raw.symbol,market,env);return {analysis:raw,quote:q};}
async function bridgeReview(env,evidence){const secret=String(env.V11_AI_BRIDGE_SECRET||'');if(!secret||!env.AI_BRIDGE||typeof env.AI_BRIDGE.fetch!=='function')return {ok:false,error:'AI_BRIDGE_UNAVAILABLE',providers:{}};try{const r=await env.AI_BRIDGE.fetch(new Request('http://127.0.0.1:8789/review',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+secret},body:JSON.stringify({evidence}),signal:AbortSignal.timeout(125000)}));const j=await r.json().catch(()=>({}));return {ok:r.ok&&j?.ok===true,providers:j?.providers||{},error:r.ok?null:`BRIDGE_HTTP_${r.status}`};}catch(e){return {ok:false,error:'BRIDGE_FETCH_'+String(e?.message||e),providers:{}};}}
function aiVote(item,side,sp){if(item?.status!=='OK'||!item.review)return 'UNAVAILABLE';const r=item.review,d=String(r.direction||'WAIT').toUpperCase(),conf=Number(r.confidence||0),hard=Array.isArray(r.hardRisk)&&r.hardRisk.length>0,min=sp?.aiMinConfidence||52;if(hard&&conf>=50)return 'CUT';if(d===side&&!hard&&conf>=min)return 'KEEP';if(d!==side&&conf>=Math.max(50,min-1))return 'CUT';return 'NEUTRAL';}
async function aiReevaluate(x,current,deterministic,env){const side=String(x.candidate?.side||'').toUpperCase(),sp=getSymbolScalpPolicy(x.symbol,x.market),evidence={mode:'V11_LIVE_POSITION_REEVALUATION',instruction:'Re-evaluate this already-open scalp MARKET signal using ONLY supplied fresh evidence. KEEP if the original side still has a reasonable immediate scalp edge. Do not CUT merely because a new scan would prefer LIMIT instead of MARKET. WAIT/opposite only when the edge is materially gone, direction conflicts, or hard risk appears. Never invent data.',market:x.market,symbol:x.symbol,symbolPolicy:sp,originalSide:side,entry:Number(x.candidate?.entry),sl:Number(x.candidate?.sl),tp:Number(x.candidate?.tp),currentPrice:Number(current.quote?.price),quote:{source:current.quote?.source,providerTimestamp:current.quote?.providerTimestamp,quoteAgeSec:current.quote?.quoteAgeSec,fresh:current.quote?.fresh},analysis:{status:current.status,qualityScore:current.candidate?.qualityScore,setup:current.plan?.setup,room:current.plan?.rr},deterministic};const r=await bridgeReview(env,evidence),votes={};let valid=0,cut=0,keep=0;for(const p of PROVIDERS){const v=aiVote(r.providers?.[p],side,sp);votes[p]=v;if(v!=='UNAVAILABLE')valid++;if(v==='CUT')cut++;if(v==='KEEP')keep++;}return {bridgeOk:r.ok,valid,cut,keep,votes,cutConfirmed:valid>=4&&cut>=4,keepConfirmed:valid>=sp.minValidAi&&keep>=sp.minAlignedAi};}
export async function scanMarketV11(env,market){const r=await engine(`/run-now?group=${market}`,env);if(!r?.ok||['BUSY','RATE_BUDGET_WAIT'].includes(String(r?.status||'')))return {status:r?.status||'SCAN_FAILED',market,approved:0};let approved=0,watch=0,rejected=0,notified=0,capped=0,promoted=0;for(const raw of r.analyses||[]){if(!ACTIONABLE.has(String(raw?.status||'').toUpperCase())||!raw?.symbol)continue;try{const {analysis,quote}=await resolveFreshAnalysis(raw,market,env),upstreamStatus=String(analysis?.status||raw?.status||'').toUpperCase(),base=normalizeCandidate({...raw,...analysis},quote),eligible=evaluateEntryEligibility(raw.symbol,market,upstreamStatus,base,quote),sp=eligible.policy;if(!eligible.ready){await recordV11(env,{status:'WATCH',market,symbol:raw.symbol,reason:eligible.reason,eligibility:eligible});watch++;continue;}const c={...base,entry:Number(quote.price),price:Number(quote.price)},plan=buildEntryPlan(market,c);if(!plan.ok||!plan.executable){await recordV11(env,{status:plan.ok?'WATCH':'REJECTED',market,symbol:raw.symbol,plan});plan.ok?watch++:rejected++;continue;}const candidate={...c,symbol:plan.symbol,entry:Number(quote.price),sl:plan.sl,tp:plan.tp,rr:plan.rr,setup:plan.setup,upstreamStatus,quote,symbolPolicy:sp,promotionMode:eligible.mode};const gate=evaluateMarketCandidate(market,candidate);if(!gate.pass){await recordV11(env,{status:'REJECTED',market,symbol:plan.symbol,reason:gate.reasons?.join('|')||'MARKET_POLICY_REJECTED',plan,gate});rejected++;continue;}const saved=await recordV11(env,{status:'APPROVED_SIGNAL',version:V11_CONFIG.version,market,symbol:plan.symbol,candidate,plan:{...plan,entry:Number(quote.price)},gate,approvedAt:Date.now(),expiresAt:Date.now()+sp.horizonMin*60000});approved++;if(eligible.mode==='LIMIT_NEAR_MARKET')promoted++;if(saved.storeReason==='MARKET_CAP_REACHED'){capped++;continue;}if(saved.stored&&await notify(env,signalText(saved)))notified++;}catch(e){await recordV11(env,{status:'REJECTED',market,symbol:raw.symbol,plan:{reason:String(e?.message||e)}});rejected++;}}return {status:'SCANNED',market,approved,watch,rejected,notified,capped,promoted,maxOpenPerMarket:5,coverage:{requested:r.requested,broadOk:r.broadOk,deepOk:r.deepOk}};}
async function lifecycleQuote(symbol,market,env){return getLiveQuote(symbol,market,env);}
async function revalidateOpenSignal(x,q,env){const {analysis,quote}=await freshAnalyze(x.symbol,x.market,env),status=String(analysis?.status||'').toUpperCase(),base=normalizeCandidate(analysis,quote),c={...base,entry:Number(quote.price),price:Number(quote.price)},plan=buildEntryPlan(x.market,c),originalSide=String(x.candidate?.side||'').toUpperCase(),newSide=String(plan?.side||c?.side||'').toUpperCase();let gate={pass:true,reasons:[]};if(plan.ok&&plan.executable){const candidate={...c,symbol:x.symbol,entry:Number(quote.price),sl:plan.sl,tp:plan.tp,rr:plan.rr,setup:plan.setup,upstreamStatus:status,side:originalSide||newSide,quote};gate=evaluateMarketCandidate(x.market,candidate);}const hold=evaluateHoldStatus(status,originalSide,newSide,plan,gate),deterministicKeep=hold.keep,reason=hold.reason;const due=Date.now()-Number(x.lastAiReviewAt||0)>=AI_RECHECK_MS;if(!due&&deterministicKeep)return {keep:true,reason:'AI_RECHECK_NOT_DUE',lastAiReviewAt:x.lastAiReviewAt,lastAiVotes:x.lastAiVotes,lastAiValid:x.lastAiValid};const ai=await aiReevaluate(x,{analysis,quote,status,candidate:c,plan},{keep:deterministicKeep,reason,gateReasons:gate.reasons||[]},env),meta={lastAiReviewAt:Date.now(),lastAiVotes:ai.votes,lastAiValid:ai.valid,lastAiCutVotes:ai.cut,lastAiKeepVotes:ai.keep};if(ai.cutConfirmed)return {keep:false,reason:`AI_4OF5_CUT:${reason}`,cutAiVotes:ai.cut,cutAiValid:ai.valid,...meta};return {keep:true,reason:ai.valid<4?`AI_INSUFFICIENT_EVIDENCE:${reason}`:ai.keepConfirmed?'AI_KEEP_CONFIRMED':`AI_NO_CUT_CONSENSUS:${reason}`,...meta};}
export async function scheduledNativeV11(event,env,ctx){ctx.waitUntil((async()=>{const lifecycle=await refreshV11Lifecycle(env,(s,m)=>lifecycleQuote(s,m,env),(x,q)=>revalidateOpenSignal(x,q,env));for(const x of lifecycle.events||[])await notify(env,lifecycleText(x));const t=Number(event?.scheduledTime||Date.now()),state=await env.TRADING_STATE?.get(SKEY,'json')||{last:{}};const dueMarkets=V11_GROUPS.map(m=>({m,due:t-Number(state.last?.[m]||0)-V11_CONFIG.scanCadenceSec[m]*1000})).filter(x=>x.due>=0).sort((a,b)=>b.due-a.due).slice(0,2);if(!dueMarkets.length)return;const results=await Promise.all(dueMarkets.map(x=>scanMarketV11(env,x.m)));for(let i=0;i<dueMarkets.length;i++)if(results[i]?.status==='SCANNED')state.last={...(state.last||{}),[dueMarkets[i].m]:t};state.updatedAt=Date.now();await env.TRADING_STATE?.put(SKEY,JSON.stringify(state));})().catch(e=>console.error('V11_NATIVE_CRON',e)));}
