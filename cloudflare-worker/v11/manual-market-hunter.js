import baseEngine from '../engine-v77168.js';
import {V11_GROUPS} from './config.js';
import {evaluateMarketCandidate} from './market-policies.js';
const MARKET_READY=new Set(['MARKET_SIGNAL','MARKET']);
const PROVIDERS=['claude','codex','deepseek','qwen','openrouter'];
async function engine(path,env){const r=await baseEngine.fetch(new Request(`https://v11.hunter${path}`),env);try{return await r.json()}catch{return null}}
async function fresh(symbol,env){const a=await engine(`/analyze?symbol=${encodeURIComponent(symbol)}`,env),q=a?.analysisQuote||a?.quote;if(!(Number(q?.price)>0)||q?.fresh!==true)throw new Error('QUOTE_NOT_VERIFIED_FRESH');return {a,q:{...q,price:Number(q.price)}};}
const finite=v=>{const x=Number(v);return Number.isFinite(x)?x:null};
function normalize(raw,a,q){const planned=a?.planned||raw?.planned||{},canonical=a?.canonical||raw?.canonical||{},mg=canonical.marketGeometry||{},lg=canonical.limitGeometry||{},m15=canonical.m15Location||{},m5=canonical.m5Trigger||{};const atrCandidates=[a?.atr,raw?.atr,a?.atr14,raw?.atr14,a?.indicators?.atr,raw?.indicators?.atr,a?.indicators?.atr14,raw?.indicators?.atr14,a?.M5?.atr14,raw?.M5?.atr14,a?.M15?.atr14,raw?.M15?.atr14,a?.H1?.atr14,raw?.H1?.atr14,a?.timeframes?.M5?.atr14,raw?.timeframes?.M5?.atr14,a?.timeframes?.M15?.atr14,raw?.timeframes?.M15?.atr14,a?.timeframes?.H1?.atr14,raw?.timeframes?.H1?.atr14,planned?.atr,planned?.atr14,mg?.atr,lg?.atr].map(finite).filter(x=>x>0);const entry=finite(a?.entry??raw?.entry??planned?.entry??mg?.entry??lg?.entry??q?.price);const structureLevels=[a?.swingLow,a?.swingHigh,a?.support,a?.resistance,raw?.swingLow,raw?.swingHigh,raw?.support,raw?.resistance,a?.structureInvalidation,raw?.structureInvalidation,a?.swingInvalidation,raw?.swingInvalidation,m15?.level,m5?.level].map(finite).filter(Number.isFinite);const liquidityLevels=[a?.liquidityLow,a?.liquidityHigh,raw?.liquidityLow,raw?.liquidityHigh,a?.target1,a?.target2,a?.tp,a?.tp1,a?.tp2,raw?.target1,raw?.target2,raw?.tp,raw?.tp1,raw?.tp2,planned?.tp,planned?.tp1,planned?.tp2].map(finite).filter(Number.isFinite);return {...raw,...a,symbol:raw.symbol,entry,price:finite(q?.price),quote:q,atr:atrCandidates[0]??null,qualityScore:Number(a?.qualityScore??raw?.qualityScore??a?.quality??raw?.quality??a?.score??raw?.score??0),structureLevels,liquidityLevels};}
function score(x){return Number(x.gate?.score||x.candidate?.qualityScore||0)+Math.min(20,Number(x.plan?.rr||0)*8);}
async function marketCandidates(env,market){const scan=await engine(`/run-now?group=${market}`,env);if(!scan?.ok)return [];const out=[];for(const raw of scan.analyses||[]){if(!raw?.symbol)continue;try{const {a,q}=await fresh(raw.symbol,env),upstreamStatus=String(a?.status||'').toUpperCase();if(!MARKET_READY.has(upstreamStatus))continue;const c=normalize(raw,a,q),entry=finite(a?.entry??q?.price),sl=finite(a?.sl??a?.planned?.sl),tp=finite(a?.tp2??a?.tp??a?.tp1??a?.planned?.tp2??a?.planned?.tp??a?.planned?.tp1),rr=finite(a?.targetRR??a?.planned?.targetRR);if(!['LONG','SHORT'].includes(String(c?.side||'').toUpperCase())||!(entry>0)||!(sl>0)||!(tp>0)||!(rr>0))continue;const setup=a?.entryPolicy?.trigger?.mode||a?.planned?.entryStyle||'UPSTREAM_MARKET_READY',candidate={...c,symbol:raw.symbol,entry,price:finite(q?.price),sl,tp,rr,setup,orderType:'MARKET',upstreamStatus,upstreamExecutionReady:a?.executionReady??null,upstreamExecutionVerified:a?.quote?.executionVerified??a?.analysisQuote?.executionVerified??null},gate=evaluateMarketCandidate(market,candidate);if(!gate.pass)continue;const plan={ok:true,executable:true,status:'READY',symbol:raw.symbol,market,side:candidate.side,setup,entry,sl,tp,rr,reason:'UPSTREAM_MARKET_READY'};out.push({market,candidate,plan,gate,quote:q,score:score({candidate,plan,gate})});}catch{}}return out.sort((a,b)=>b.score-a.score).slice(0,3);}
async function bridge(env,evidence){const secret=String(env.V11_AI_BRIDGE_SECRET||'');if(!env.AI_BRIDGE||typeof env.AI_BRIDGE.fetch!=='function')return {ok:false,error:'V11_AI_BRIDGE_VPC_BINDING_MISSING',providers:{}};if(!secret)return {ok:false,error:'V11_AI_BRIDGE_SECRET_MISSING',providers:{}};try{const request=new Request('http://127.0.0.1:8789/review',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+secret},body:JSON.stringify({evidence}),signal:AbortSignal.timeout(125000)});const r=await env.AI_BRIDGE.fetch(request);let j={};try{j=await r.json();}catch{return {ok:false,error:'BRIDGE_INVALID_JSON',providers:{}};}return r.ok?j:{ok:false,error:'BRIDGE_HTTP_'+r.status,providers:j?.providers||{}};}catch(e){return {ok:false,error:'BRIDGE_VPC_FETCH_FAILED:'+String(e?.message||e),providers:{}};}}
function good(r,side){return r?.status==='OK'&&r.review&&r.review.direction===side&&!r.review.hardRisk?.length;}
export async function huntWholeMarket(env){const groups=await Promise.all(V11_GROUPS.map(m=>marketCandidates(env,m)));const candidates=groups.flat().sort((a,b)=>b.score-a.score).slice(0,5);if(!candidates.length)return {status:'NO_MARKET_ENTRY',reason:'NO_FRESH_MARKET_CANDIDATE',candidates:[],shortlist:[],reviewMode:'ON_DEMAND_VPC_BRIDGE_5AI',automaticSignalAuthority:false,providerCount:5,consensusRequired:4};const reviewed=[];for(const x of candidates){const evidence={mode:'MANUAL_WHOLE_MARKET_MARKET_HUNTER',market:x.market,candidate:x.candidate,entryPlan:x.plan,quote:x.quote,instruction:'Independent second opinion for an immediate MARKET entry. Never invent missing evidence.'};const vps=await bridge(env,evidence),side=String(x.candidate.side).toUpperCase(),ai={};for(const p of PROVIDERS)ai[p]=vps.providers?.[p]||{status:'UNAVAILABLE'};const alignedProviders=PROVIDERS.filter(p=>good(ai[p],side)),consensus=alignedProviders.length>=4;reviewed.push({...x,ai,alignedProviders,consensus,consensusCount:alignedProviders.length});}const conf=x=>PROVIDERS.reduce((s,p)=>s+Number(x.ai?.[p]?.review?.confidence||0),0)+x.score,aligned=reviewed.filter(x=>x.consensus).sort((a,b)=>conf(b)-conf(a)),best=aligned[0]||reviewed.sort((a,b)=>b.score-a.score)[0];return {status:aligned.length?'MARKET_ENTRY_FOUND':'NO_5AI_CONSENSUS',best,shortlist:reviewed,reviewMode:'ON_DEMAND_VPC_BRIDGE_5AI',automaticSignalAuthority:false,providerCount:5,consensusRequired:4};}
