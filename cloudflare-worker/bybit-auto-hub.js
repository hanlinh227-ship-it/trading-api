import {telegramApiRequest} from "./providers/telegram-client.js";
import {getBtcHyperscaleState} from "./bybit-btc-engine.js";
import {capitalBaseState,equityScaleState} from "./bybit-btc-risk-engine.js";
import {bybitExecutionMode,BYBIT_AUTO_VERSION,bybitAutoConfig} from "./bybit-auto-config.js";
import {bybitV5} from "./bybit-v5-client.js";

const json=(b,s=200)=>new Response(JSON.stringify(b,null,2),{status:s,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const fmt=(v,d=2)=>Number.isFinite(Number(v))?Number(v).toFixed(d):"—";
const money=v=>Number.isFinite(Number(v))?`${Number(v)>=0?"+":""}$${Number(v).toFixed(2)}`:"—";
const px=v=>Number.isFinite(Number(v))?Number(v).toFixed(Number(v)>=100?1:4).replace(/0+$/,'').replace(/\.$/,''):"—";
const auth=(u,e)=>{const g=String(u?.callback_query?.from?.id??u?.message?.from?.id??""),w=String(e.TELEGRAM_ALLOWED_USER_ID||e.TELEGRAM_CHAT_ID||"");return !w||g===w;};
const menu=()=>({inline_keyboard:[[{text:"📊 BTC DASHBOARD",callback_data:"btc:dashboard"}],[{text:"💰 ACCOUNT / SCALE",callback_data:"btc:account"}],[{text:"📌 POSITION",callback_data:"btc:position"},{text:"🧩 TRANCHES",callback_data:"btc:tranches"}],[{text:"🛡️ RISK / SL",callback_data:"btc:risk"},{text:"⚙️ RUNTIME",callback_data:"btc:runtime"}],[{text:"🔄 REFRESH",callback_data:"btc:dashboard"}]]});
async function send(e,id,text){return telegramApiRequest(e,"sendMessage",{chat_id:id||e.TELEGRAM_CHAT_ID,text,reply_markup:menu(),disable_web_page_preview:true});}

async function snap(e){const state=await getBtcHyperscaleState(e),api=bybitV5(e),cfg=bybitAutoConfig(e),[w,p,o]=await Promise.allSettled([api.wallet(),api.positions(),api.openOrders()]),acct=w.status==="fulfilled"?(w.value?.result?.list?.[0]||{}):{},coin=(acct.coin||[]).find(x=>x.coin==="USDT")||{},positions=p.status==="fulfilled"?(p.value?.result?.list||[]).filter(x=>x.symbol==="BTCUSDT"&&Number(x.size)>0):[],orders=o.status==="fulfilled"?(o.value?.result?.list||[]).filter(x=>x.symbol==="BTCUSDT"&&!['Filled','Cancelled','Rejected','Deactivated'].includes(String(x.orderStatus))):[],account={equity:Number(acct.totalEquity||coin.equity||0),wallet:Number(acct.totalWalletBalance||coin.walletBalance||0),available:Number(acct.totalAvailableBalance||coin.availableToWithdraw||0),initialMargin:Number(acct.totalInitialMargin||0)};const capital=capitalBaseState({equityUsd:account.equity,walletBalanceUsd:account.wallet,cfg}),scale=equityScaleState(capital.capitalBaseUsd,cfg);return {state,cfg,mode:bybitExecutionMode(e),account,capital,scale,positions,orders};}
function active(s){return Array.isArray(s.state?.tranches)?s.state.tranches.filter(x=>String(x.status||"OPEN")==="OPEN"):[];}
function activeRisk(s){return active(s).reduce((z,t)=>{const q=Math.abs(Number(t.qty||0)),e=Number(t.entry||0),sl=Number(t.managedSl||t.sl||0),side=String(t.side||"");if(!(q>0&&e>0&&sl>0))return z+Math.max(0,Number(t.riskUsd||0));if(side==="Buy"&&sl>=e)return z;if(side==="Sell"&&sl<=e)return z;return z+Math.abs(e-sl)*q;},0);}
function nextTier(s){const rows=[...(s.cfg?.risk?.equityScale?.steps||[])].sort((a,b)=>Number(a.equityUsd)-Number(b.equityUsd));return rows.find(x=>Number(x.equityUsd)>Number(s.capital?.capitalBaseUsd||0))||null;}
function protectionLabel(p,t){if(!p)return "FLAT";const sl=Number(p.stopLoss||0),entry=Number(p.avgPrice||0),side=String(p.side||"");if(!(sl>0))return "⚠️ NO NATIVE SL";const be=side==="Buy"?sl>=entry:sl<=entry;if(be)return "🔒 BE/PROFIT PROTECTED";const latest=t[t.length-1],orig=Number(latest?.sl||0);if(orig>0&&Math.abs(sl-entry)<Math.abs(orig-entry))return "🛡️ TRAILING/TIGHTENED";return "🛡️ INITIAL SL";}
function dash(s){const p=s.positions[0],t=active(s),risk=activeRisk(s),eq=s.account.equity,riskPct=eq>0?risk/eq*100:0,n=nextTier(s);return ["₿ BTCUSDT HYPERSCALE",`${BYBIT_AUTO_VERSION} • ${s.mode}`,"",`💰 Balance $${fmt(s.account.wallet)} • Equity $${fmt(eq)}`,`💵 Available $${fmt(s.account.available)} • Capital base $${fmt(s.capital.capitalBaseUsd)}`,`📈 Scale tier $${fmt(s.scale.tierEquityUsd,0)} • Risk ×${fmt(s.scale.riskMult,2)}${n?` • next $${fmt(n.equityUsd,0)}`:" • MAX TIER"}`,`📌 ${p?`${String(p.side).toUpperCase()} ${p.size} BTC @ ${px(p.avgPrice)}`:"FLAT"}`,p?`${protectionLabel(p,t)} • SL ${px(p.stopLoss)} • PnL ${money(p.unrealisedPnl)}`:null,`🧩 Tranches ${t.length} • active risk ${usdRisk(risk)} (${fmt(riskPct,2)}%)`,`🌐 Regime ${s.state?.lastRegime||"—"}`,"",`Entry alerts ON • BE / trailing / smart-cut alerts ON`].filter(Boolean).join("\n");}
const usdRisk=v=>`$${Math.abs(Number(v||0)).toFixed(2)}`;
function account(s){const n=nextTier(s),u=Math.max(0,Number(s.account.equity)-Number(s.account.wallet));return ["💰 BTC ACCOUNT / SCALE",`Wallet balance $${fmt(s.account.wallet)}`,`Equity $${fmt(s.account.equity)} • Available $${fmt(s.account.available)}`,`Unrealized PnL ${money(s.account.equity-s.account.wallet)}`,`Scale capital $${fmt(s.capital.capitalBaseUsd)} (${fmt(s.capital.creditPct,0)}% unrealized-profit credit)`,`Current tier $${fmt(s.scale.tierEquityUsd,0)} • risk ×${fmt(s.scale.riskMult,2)}`,`Margin cap ${fmt(s.scale.marginCapPct,0)}% • leverage bonus +${fmt(s.scale.leverageBonus,0)}x`,n?`Next tier $${fmt(n.equityUsd,0)} • còn $${fmt(Math.max(0,Number(n.equityUsd)-Number(s.capital.capitalBaseUsd)))}`:"🏁 Highest configured scale tier reached",`High-water $${fmt(s.state?.highWaterUsd||s.account.equity)}`,`External cashflow cumulative ${money(s.state?.cumulativeExternalCashFlowUsd||0)}`].join("\n");}
function position(s){const p=s.positions[0],t=active(s);if(!p)return "📌 BTC POSITION\n\n⚪ FLAT";return ["📌 BTC POSITION",`${String(p.side).toUpperCase()} ${p.size} BTC`,`Entry ${px(p.avgPrice)} → Mark ${px(p.markPrice)}`,`${protectionLabel(p,t)}`,`Native SL ${px(p.stopLoss)} • TP ${px(p.takeProfit)}`,`PnL ${money(p.unrealisedPnl)} • IM $${fmt(p.positionIM||0,4)}`,`Leverage ${p.leverage}x • Open orders ${s.orders.length}`,`Tranches ${t.length}`].join("\n");}
function tranches(s){const t=active(s);if(!t.length)return "🧩 BTC TRANCHES\n\n⚪ Không có tranche mở.";return ["🧩 BTC TRANCHES",...t.slice(-12).reverse().map((x,i)=>`${i+1}. ${x.side} ${fmt(x.qty,6)} @ ${px(x.entry)}\n   Initial SL ${px(x.sl)} → Managed SL ${px(x.managedSl||x.sl)}\n   Virtual TP ${px(x.tp)} • ${x.setup||"SETUP"}\n   ${x.protected?"🔒 PROTECTED / RISK RECYCLED":"🟠 RISK ACTIVE"}`)].join("\n\n");}
function risk(s){const r=s.cfg.risk,t=active(s),ar=activeRisk(s),eq=s.account.equity,dd=s.state?.highWaterUsd>0?(s.state.highWaterUsd-eq)/s.state.highWaterUsd*100:0;return ["🛡️ BTC RISK / PROTECTION",`Balance $${fmt(s.account.wallet)} • Equity $${fmt(eq)}`,`Capital base $${fmt(s.capital.capitalBaseUsd)} • tier $${fmt(s.scale.tierEquityUsd,0)}`,`Drawdown ${fmt(Math.max(0,dd),2)}%`,`Active risk $${fmt(ar)} / ${fmt(r.maxActiveRiskPct,1)}% normal cap`,`Single risk ${r.baseEntryRiskPct}% normal • ${r.strongEntryRiskPct}% strong • ${r.aPlusEntryRiskPct}% A+`,`Dynamic margin cap ${fmt(s.scale.marginCapPct,0)}% • reserve ≥${r.minFreeReservePct}%`,`Protection: native SL → BE/profit-lock → trailing → smart cut`,`Winner pyramid only after prior risk protection`,`Open tranches ${t.length} • daily quota NONE`].join("\n");}
function runtime(s){return ["⚙️ BTC RUNTIME",`${BYBIT_AUTO_VERSION} • ${s.mode}`,`Symbol BTCUSDT Linear Perpetual`,`Entry notifications: LIVE fills`,`Lifecycle notifications: BE / PROFIT_LOCK / TRAIL / SMART CUT`,`Account telemetry: balance / equity / available / scale tier`,`Legacy multi-coin / Forex / Meme execution retired`,`Last cycle ${s.state?.lastCycleAt||"—"}`,`Last entry ${s.state?.lastEntryAt||"—"}`,`Last setup ${s.state?.lastEntrySetup||"—"}`].join("\n");}

export default {async fetch(req,e){const u=new URL(req.url);if(u.pathname==="/auto-hub/status"){const s=await snap(e);return json({ok:true,service:"BTC_ONLY_TRADING_HUB",version:BYBIT_AUTO_VERSION,mode:s.mode,symbol:"BTCUSDT",balanceUsd:s.account.wallet,equityUsd:s.account.equity,availableUsd:s.account.available,capitalBaseUsd:s.capital.capitalBaseUsd,scale:s.scale,nextTier:nextTier(s),activeTranches:active(s).length,position:s.positions[0]||null});}if(u.pathname==="/telegram/webhook"&&req.method==="POST"){let x;try{x=await req.json();}catch{return json({ok:false,error:"BAD_JSON"},400);}if(!auth(x,e))return json({ok:false,error:"FORBIDDEN"},403);const cb=String(x?.callback_query?.data||""),msg=String(x?.message?.text||""),id=x?.callback_query?.message?.chat?.id??x?.message?.chat?.id??e.TELEGRAM_CHAT_ID;if(!cb&&!["/start","/auto","/hub","/btc"].includes(msg))return json({ok:true,ignored:true});const s=await snap(e);let text=dash(s);if(cb==="btc:account")text=account(s);else if(cb==="btc:position")text=position(s);else if(cb==="btc:tranches")text=tranches(s);else if(cb==="btc:risk")text=risk(s);else if(cb==="btc:runtime")text=runtime(s);await send(e,id,text);return json({ok:true,branch:"BTC",version:BYBIT_AUTO_VERSION,mode:s.mode});}if(u.pathname==="/"||u.pathname==="/hub"||u.pathname==="/auto-hub")return json({ok:true,service:"BTC_ONLY_TRADING_HUB",version:BYBIT_AUTO_VERSION,telegram:"/telegram/webhook",symbol:"BTCUSDT"});return null;}};
