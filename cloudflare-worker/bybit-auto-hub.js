import {telegramApiRequest} from "./providers/telegram-client.js";
import {getBybitAutoV1State} from "./bybit-auto-v1.js";
import {getBybitLearningState} from "./bybit-learning-engine.js";
import {probeBybitAiBridge} from "./bybit-ai-scalp-gate.js";
import {bybitExecutionMode,BYBIT_AUTO_VERSION,bybitAutoConfig} from "./bybit-auto-config.js";
import {bybitV5} from "./bybit-v5-client.js";
import {MEME_AUTO_VERSION,MEME_AUTO_MODE,MEME_AUTO_DESIGN,getMemeAutoDesignStatus} from "./meme-auto-design.js";

const json=(body,status=200)=>new Response(JSON.stringify(body,null,2),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const fmt=(v,d=2)=>Number.isFinite(Number(v))?Number(v).toFixed(d):"—";
const money=v=>Number.isFinite(Number(v))?`${Number(v)>=0?"+":""}$${Number(v).toFixed(2)}`:"—";
function px(v,tick=0){const n=Number(v);if(!Number.isFinite(n))return "—";const t=Math.abs(Number(tick||0));let d=8;if(t>0){const s=t.toFixed(12).replace(/0+$/,"");const p=s.indexOf(".");d=p<0?0:s.length-p-1;}else if(Math.abs(n)>=100)d=2;else if(Math.abs(n)>=1)d=4;else if(Math.abs(n)>=.01)d=5;else d=6;return n.toFixed(Math.min(8,d)).replace(/(\.\d*?[1-9])0+$|\.0+$/,"$1");}
const auth=(u,env)=>{const got=String(u?.callback_query?.from?.id??u?.message?.from?.id??""),want=String(env.TELEGRAM_ALLOWED_USER_ID||env.TELEGRAM_CHAT_ID||"");return !want||got===want;};
const rootMenu=()=>({inline_keyboard:[[{text:"🟢 BYBIT",callback_data:"hub:bybit"},{text:"🟣 MEME",callback_data:"hub:meme"}],[{text:"🔄 HUB",callback_data:"hub:home"}]]});
const bybitMenu=()=>({inline_keyboard:[[{text:"🤖 AUTO DASHBOARD",callback_data:"auto:dashboard"}],[{text:"📌 VỊ THẾ LIVE",callback_data:"auto:positions"}],[{text:"🧠 3 AI",callback_data:"auto:ai"},{text:"🛡️ RISK / SAFETY",callback_data:"auto:risk"}],[{text:"📈 PNL / LEARNING",callback_data:"auto:stats"},{text:"⚙️ RUNTIME",callback_data:"auto:runtime"}],[{text:"⬅️ HUB",callback_data:"hub:home"},{text:"🔄 REFRESH",callback_data:"auto:dashboard"}]]});
const memeMenu=()=>({inline_keyboard:[[{text:"🧬 MEME DESIGN",callback_data:"meme:dashboard"}],[{text:"🛡️ SAFETY",callback_data:"meme:safety"},{text:"🎯 ENTRY / EXIT",callback_data:"meme:trade"}],[{text:"💰 CAPITAL",callback_data:"meme:capital"},{text:"📚 LEARNING",callback_data:"meme:learning"}],[{text:"⬅️ HUB",callback_data:"hub:home"},{text:"🔄 REFRESH",callback_data:"meme:dashboard"}]]});
async function send(env,id,text,markup=rootMenu()){return telegramApiRequest(env,"sendMessage",{chat_id:id||env.TELEGRAM_CHAT_ID,text,reply_markup:markup,disable_web_page_preview:true});}
async function snapshot(env){const state=await getBybitAutoV1State(env),api=bybitV5(env),cfg=bybitAutoConfig(env);const [w,p,o,ai,l]=await Promise.allSettled([api.wallet(),api.positions(),api.openOrders(),probeBybitAiBridge(env),getBybitLearningState(env)]);const acct=w.status==="fulfilled"?(w.value?.result?.list?.[0]||{}):{},coin=(acct.coin||[]).find(x=>x.coin==="USDT")||{},positions=p.status==="fulfilled"?(p.value?.result?.list||[]).filter(x=>Number(x.size||0)>0):[],orders=o.status==="fulfilled"?(o.value?.result?.list||[]).filter(x=>!["Filled","Cancelled","Rejected","Deactivated"].includes(String(x.orderStatus))):[];return {state,mode:bybitExecutionMode(env),cfg,account:{equity:Number(acct.totalEquity||coin.equity||0),wallet:Number(acct.totalWalletBalance||coin.walletBalance||0),available:Number(acct.totalAvailableBalance||coin.availableToWithdraw||0),marginBalance:Number(acct.totalMarginBalance||0),initialMargin:Number(acct.totalInitialMargin||0),imRate:Number(acct.accountIMRate||0)},positions,openOrders:orders,ai:ai.status==="fulfilled"?ai.value:{ok:false,error:String(ai.reason||"AI_PROBE_FAILED")},learning:l.status==="fulfilled"?l.value:{summary:{}},checkedAt:new Date().toISOString()};}
function hubText(env){return ["🧭 UNIFIED TRADING HUB","Chọn hệ thống:",`🟢 BYBIT • ${BYBIT_AUTO_VERSION} • ${bybitExecutionMode(env)}`,`🟣 MEME • ${MEME_AUTO_VERSION} • ${MEME_AUTO_MODE}`,"","BYBIT đang là execution authority LIVE.","MEME hiện DESIGN ONLY • chưa ví • chưa ký • chưa BUY/SELL."].join("\n");}
function positionLines(s){if(!s.positions.length)return ["⚪ Không có vị thế LIVE."];return s.positions.map(x=>{const plan=s.state?.openPlans?.[x.symbol]||{},review=plan.lastReview||{},tick=Number(plan.tickSize||plan.filters?.tickSize||0),risk=Number(plan.riskUsd||0),reward=Number(plan.rewardUsd||0);return `${String(x.side).toLowerCase()==="buy"?"🟢":"🔴"} ${x.symbol} ${String(x.side).toUpperCase()}\nE ${px(x.avgPrice||plan.entry,tick)} • Mark ${px(x.markPrice,tick)}\nSL ${px(x.stopLoss||plan.managedSl||plan.sl,tick)} • -$${risk.toFixed(2)}\nTP ${px(x.takeProfit||plan.tp,tick)} • +$${reward.toFixed(2)}\nPnL ${money(x.unrealisedPnl)} • ${review.verdict||plan.managementPhase||"HOLD"}${Number.isFinite(Number(review.r))?` • R ${fmt(review.r,2)}`:""}`;});}
function dashboard(s){const ai=s.ai||{},learn=s.learning?.summary||{};return ["🤖 BYBIT AUTO TRADE HUB",`${BYBIT_AUTO_VERSION} • ${s.mode}`,`🟢 ${s.mode} • ${String(s.state?.executionMode||"UNKNOWN")} • ${Number(s.state?.pauseUntil||0)>Date.now()?"PAUSED":"RUNNING"}`,`🧬 Adaptive Edge ON • regime + coin edge + correlation`,`🎯 Score adaptive ${s.cfg.adaptive.minScore}–${s.cfg.adaptive.maxScore} • auto-promote OFF`,`♾️ Continuous trading • Daily target OFF`,`🧮 Capital allocator • reserve ${fmt(s.cfg.risk.minFreeReservePct,0)}% • slot IM ≤${fmt(s.cfg.risk.maxMarginPerPositionPct,0)}%`,`✂️ Smart CUT ON • multi-signal confirmation`,`💰 Balance $${fmt(s.account.wallet)}`,`📊 Equity $${fmt(s.account.equity)} • Available $${fmt(s.account.available)}`,`🏦 Initial Margin $${fmt(s.account.initialMargin)} • IM rate ${fmt(s.account.imRate*100,1)}%`,`📌 Position ${s.positions.length} • Orders ${s.openOrders.length}`,`📈 Realized ${money(s.state?.realizedUsd||0)} • Learned ${Number(learn.sampleSize||0)}`,`🧠 AI ${ai.ok?"READY":"DEGRADED"} • ${Number(ai.online||0)}/${Number(ai.configured||3)} • ${ai.bridgeMode||"—"}`,`🛡️ Loss streak ${Number(s.state?.lossStreak||0)}`,`🕒 ${s.checkedAt}`].join("\n");}
function aiText(s){const a=s.ai||{},ps=a.providers||{};return ["🧠 3 AI CORE",`Bridge: ${a.ok?"✅ READY":"⚠️ DEGRADED"} • ${a.bridgeMode||"—"}`,`Claude: ${ps.claude?.state||"—"}`,`Codex/ChatGPT: ${ps.codex?.state||"—"}`,`DeepSeek: ${ps.deepseek?.state||"—"}`,`Grace ${a.fastFirstGraceSec??"—"}s • budget ${a.bridgeBudgetSec??"—"}s`,s.state?.lastAiReview?`Last: ${s.state.lastAiReview.symbol} ${s.state.lastAiReview.side} • ${s.state.lastAiReview.mode} • usable ${s.state.lastAiReview.usable}`:"Last review: —"].join("\n");}
function riskText(s){const lp=s.state?.lastPnlReconcile||{},pm=s.state?.lastPositionManagement||{},last=pm.results?.at?.(-1)||{},r=s.cfg.risk,a=s.cfg.adaptive;return ["🛡️ RISK / SAFETY",`Mode ${s.mode} • ${Number(s.state?.pauseUntil||0)>Date.now()?"⏸ PAUSED":"▶️ RUNNING"}`,"Daily target: OFF • continuous trading",`Capital: slot IM ≤${fmt(r.maxMarginPerPositionPct,0)}% • reserve ≥${fmt(r.minFreeReservePct,0)}% • portfolio target ≤${fmt(r.maxPortfolioMarginPct,0)}%`,`Risk: ≤${fmt(r.maxRiskPctOfEquity,0)}%/trade • ≤${fmt(r.maxTotalOpenRiskPct,0)}% total`,`Adaptive: score ${a.minScore}–${a.maxScore} • corr ${fmt(a.correlationSoft,2)}/${fmt(a.correlationHard,2)} • no auto-promote`,`Smart CUT: ON • score ${r.smartCutScore} • ${r.smartCutConfirmations} confirmations`,`Loss streak ${Number(s.state?.lossStreak||0)}`,`Realized ${money(s.state?.realizedUsd||0)}`,`Open plans ${Object.keys(s.state?.openPlans||{}).length}`,`PnL reconcile ${lp.at||"—"}`,`Position review ${pm.at||"—"}${last.smartCut?.score!==undefined?` • CUT score ${last.smartCut.score}/${last.smartCut.scoreNeed}`:""}`,"fresh quote • regime • coin edge • correlation • 3AI • native SL/TP • SMART_CUT"].join("\n");}
async function statsText(env,s){const l=s.learning||{},sm=l.summary||{},symbols=Object.keys(sm.bySymbol||{});return ["📈 PNL / LEARNING",`Realized ${money(s.state?.realizedUsd||0)} • Trades ${Number(s.state?.trades||0)}`,`Closed ${Number(s.state?.lastPnlReconcile?.closedTrades||0)}`,`Learning ${Number(sm.sampleSize||0)} • symbols ${symbols.length} • ${l.providerSet||"AUTO_CORE_3"}`,`Avg R ${fmt(sm.avgR,2)} • Net R ${fmt(sm.avgNetR,2)} • Win ${Number.isFinite(Number(sm.winRate))?fmt(Number(sm.winRate)*100,1)+"%":"—"}`,`Adaptive memory: per-coin + regime + after-cost expectancy`,`Auto-promote: OFF`,`Last trade ${s.state?.lastTradeAt?new Date(Number(s.state.lastTradeAt)).toISOString():"—"}`].join("\n");}
function runtimeText(s){const pm=s.state?.lastPositionManagement?.results||[],last=pm.at?.(-1)||{};return ["⚙️ AUTO RUNTIME",`${BYBIT_AUTO_VERSION} • ${s.mode}`,"Adaptive Edge Engine • bounded learning","Regime gate + correlation gate • ACTIVE","Continuous Capital Allocation • daily target OFF","Smart CUT ON • adaptive thesis invalidation",`Execution ${s.mode} / ${s.state?.executionMode||"UNKNOWN"}`,`Positions ${s.positions.length} • orders ${s.openOrders.length}`,`Balance $${fmt(s.account.wallet)} • Equity $${fmt(s.account.equity)} • Available $${fmt(s.account.available)}`,`Initial Margin $${fmt(s.account.initialMargin)} • IM rate ${fmt(s.account.imRate*100,1)}%`,`AI ${s.ai?.ok?"OK":"DEGRADED"}`,`Leverage cap ${s.cfg.maxLeverage}x`,`Last AI ${s.state?.lastAiReview?.at||"—"}`,`Last quote ${s.state?.lastPostAiQuote?.at||"—"}`,`Last manager ${s.state?.lastPositionManagement?.at||"—"}${last.verdict?` • ${last.verdict}`:""}`,`Telegram entry ${s.state?.lastTelegramEntryAt||"—"}`,`Checked ${s.checkedAt}`].join("\n");}
function memeDashboard(){const d=MEME_AUTO_DESIGN;return ["🟣 MEME AUTO",`${MEME_AUTO_VERSION} • ${MEME_AUTO_MODE}`,"🔒 NO WALLET • NO SIGNING • NO EXECUTION","🧠 Confirmed momentum • blind sniping OFF",`💰 Design capital $${d.startingCapitalUsd} • reserve $${d.capital.reserveUsd} • 1 position`, `🎯 Position $${d.capital.minPositionUsd}–$${d.capital.maxPositionUsd} • target $${d.capital.targetPositionUsd}`,`🛡️ Safety hard-gate before score • liquidity ≥$${Math.round(d.hardSafety.minLiquidityUsd/1000)}k`,`📊 Score WATCH ${d.qualityScore.watchScore}+ • ENTRY ${d.qualityScore.entryScore}+ • PREMIUM ${d.qualityScore.premiumScore}+`,`🌊 Entry regimes: ${d.allowedEntryRegimes.join(" / ")}`,"🚫 No leverage • no DCA • no averaging down • no martingale","Execution integration intentionally locked until wallet/data phase."].join("\n");}
function memeSafety(){const d=MEME_AUTO_DESIGN;return ["🛡️ MEME SAFETY DESIGN",`Liquidity ≥$${Math.round(d.hardSafety.minLiquidityUsd/1000)}k • fast breakout ≥$${Math.round(d.hardSafety.minLiquidityForFastBreakoutUsd/1000)}k`,`Top10 ≤${d.hardSafety.maxTop10HolderPct}%`,`Dev+insider ≤${d.hardSafety.maxDevInsiderSupplyPct}% • bundler ≤${d.hardSafety.maxBundlerSupplyPct}% • sniper ≤${d.hardSafety.maxSniperSupplyPct}%`,"Wallet-level holders required","Bundler/sniper/insider/dev/smart-trader profile required","Fresh SELL route required before BUY","Reject mint/freeze/critical security risk","Safety cannot be overridden by AI or learning."].join("\n");}
function memeTrade(){const d=MEME_AUTO_DESIGN;return ["🎯 MEME ENTRY / EXIT DESIGN","Setups: MOMENTUM_RETEST > FRESH_BREAKOUT > EARLY_ROTATION",`ENTRY score ≥${d.qualityScore.entryScore} • premium ≥${d.qualityScore.premiumScore}`,`Normal Smart CUT ${d.exits.initialCutPctRange[0]}–${d.exits.initialCutPctRange[1]}% • hard loss ${d.exits.hardLossPct}%`,`TP1 +${d.exits.tp1.gainPct}% sell ${d.exits.tp1.sellPct}%`,`TP2 +${d.exits.tp2.gainPct}% sell ${d.exits.tp2.sellPct}%`,`Then principal recovery + volatility runner`,"Emergency exit: sell-route loss / liquidity shock / security deterioration / dev dump / extreme exit impact","No forced trade quota; bad regime = NO_ENTRY."].join("\n");}
function memeCapital(){const d=MEME_AUTO_DESIGN;return ["💰 MEME CAPITAL DESIGN",`Start $${d.startingCapitalUsd}`,`Reserve $${d.capital.reserveUsd} • tradable $${d.capital.tradableUsd}`,`1 position max • target $${d.capital.targetPositionUsd}`,`Range $${d.capital.minPositionUsd}–$${d.capital.maxPositionUsd} • max allocation ${d.capital.maxAllocationPct}%`,"Spot only • leverage OFF","DCA OFF • averaging down OFF • martingale OFF","Scale only after real net-expectancy evidence."].join("\n");}
function memeLearning(){const d=MEME_AUTO_DESIGN;return ["📚 MEME LEARNING DESIGN","Memory: token + launch source + age + regime + setup + liquidity bucket","Metrics: net PnL/R • MFE/MAE • hold • entry/exit impact • fees • exit reason",`Adaptive entry score bounded ${d.qualityScore.learningBounds.minEntryScore}–${d.qualityScore.learningBounds.maxEntryScore}`,`No influence before ${d.qualityScore.learningBounds.minClosedSamples} closed samples • full weight at ${d.qualityScore.learningBounds.fullWeightSamples}+`,`Auto-promote OFF`,`Learning can tune bounded score/priority/size/exit profile only. Hard safety and wallet authority are immutable.`].join("\n");}

export default {async fetch(req,env){const u=new URL(req.url);if(u.pathname==="/meme-auto/design")return json(getMemeAutoDesignStatus());if(u.pathname==="/auto-hub/status")return json({ok:true,service:"UNIFIED_TRADING_HUB",telegramBranches:["BYBIT","MEME"],bybit:{version:BYBIT_AUTO_VERSION,mode:bybitExecutionMode(env),executionAuthority:true},meme:{version:MEME_AUTO_VERSION,mode:MEME_AUTO_MODE,executionEnabled:false,walletConnected:false},continuousTrading:true,dailyTarget:false,capitalAllocator:true,adaptiveEdge:true,smartCut:true,readOnly:true,signalHub:false});if(u.pathname==="/telegram/webhook"&&req.method==="POST"){let update;try{update=await req.json();}catch{return json({ok:false,error:"BAD_JSON"},400);}if(!auth(update,env))return json({ok:false,error:"FORBIDDEN"},403);const cb=String(update?.callback_query?.data||""),msg=String(update?.message?.text||"");if(!(cb.startsWith("auto:")||cb.startsWith("hub:")||cb.startsWith("meme:")||["/start","/auto","/hub"].includes(msg)))return json({ok:false,error:"UNIFIED_HUB_ONLY"},404);const id=update?.callback_query?.message?.chat?.id??update?.message?.chat?.id??env.TELEGRAM_CHAT_ID;if(cb==="hub:home"||(!cb&&["/start","/auto","/hub"].includes(msg))){await send(env,id,hubText(env),rootMenu());return json({ok:true,owner:"UNIFIED_TRADING_HUB",branch:"HOME",bybitVersion:BYBIT_AUTO_VERSION,memeVersion:MEME_AUTO_VERSION});}if(cb==="hub:meme"||cb.startsWith("meme:")){let text=memeDashboard();if(cb==="meme:safety")text=memeSafety();else if(cb==="meme:trade")text=memeTrade();else if(cb==="meme:capital")text=memeCapital();else if(cb==="meme:learning")text=memeLearning();await send(env,id,text,memeMenu());return json({ok:true,owner:"UNIFIED_TRADING_HUB",branch:"MEME",version:MEME_AUTO_VERSION,mode:MEME_AUTO_MODE,executionEnabled:false,walletConnected:false});}const s=await snapshot(env);if(cb==="auto:positions")await send(env,id,["📌 VỊ THẾ LIVE",...positionLines(s)].join("\n\n"),bybitMenu());else if(cb==="auto:ai")await send(env,id,aiText(s),bybitMenu());else if(cb==="auto:risk")await send(env,id,riskText(s),bybitMenu());else if(cb==="auto:stats")await send(env,id,await statsText(env,s),bybitMenu());else if(cb==="auto:runtime")await send(env,id,runtimeText(s),bybitMenu());else await send(env,id,dashboard(s),bybitMenu());return json({ok:true,owner:"UNIFIED_TRADING_HUB",branch:"BYBIT",version:BYBIT_AUTO_VERSION,mode:s.mode,continuousTrading:true,dailyTarget:false,capitalAllocator:true,adaptiveEdge:true,smartCut:true,readOnly:true});}if(u.pathname==="/"||u.pathname==="/hub"||u.pathname==="/auto-hub")return json({ok:true,service:"UNIFIED_TRADING_HUB",telegram:"/telegram/webhook",telegramBranches:["BYBIT","MEME"],bybit:{version:BYBIT_AUTO_VERSION,mode:bybitExecutionMode(env),executionAuthority:true,liveState:true},meme:{version:MEME_AUTO_VERSION,mode:MEME_AUTO_MODE,executionEnabled:false,walletConnected:false,designEndpoint:"/meme-auto/design"},signalHub:false});return null;}};
