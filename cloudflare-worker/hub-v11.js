import baseEngine from './engine-v77168.js';
import {telegramApiRequest} from './providers/telegram-client.js';
import {getV11Accepted,getV11History,getV11Funnel} from './v11/store.js';
import {scanMarketV11,scheduledNativeV11} from './v11/native-runtime.js';
import {huntWholeMarket} from './v11/manual-market-hunter.js';
import {getLiveQuote} from './v11/live-quote.js';
import {INSTRUMENT_PROFILES,canonicalInstrument,instrumentStrategy} from './v11/instrument-profiles.js';

const json=(x,s=200)=>new Response(JSON.stringify(x,null,2),{status:s,headers:{'content-type':'application/json','cache-control':'no-store'}});
const kb=()=>({inline_keyboard:[
 [{text:'🪙 CRYPTO',callback_data:'v11:market:crypto'},{text:'💱 FOREX',callback_data:'v11:market:forex'}],
 [{text:'🟡 GOLD / METAL',callback_data:'v11:market:metal'},{text:'📈 INDEX / FUTURES',callback_data:'v11:market:index'}],
 [{text:'🔎 TRA SYMBOL',callback_data:'v11:symbol:help'},{text:'📋 ALL SYMBOLS',callback_data:'v11:symbol:list'}],
 [{text:'🔥 TOP ENTRY NOW',callback_data:'v11:ai:hunt'}],
 [{text:'📚 LỆNH ĐANG CHẠY',callback_data:'v11:live'},{text:'👀 WATCHLIST',callback_data:'v11:watch'}],
 [{text:'📈 THỐNG KÊ',callback_data:'v11:stats'},{text:'🕘 LỊCH SỬ',callback_data:'v11:history'}],
 [{text:'📊 SYSTEM STATUS',callback_data:'v11:system'}],
 [{text:'🟨 BINANCE AUTO',callback_data:'binance'}]
]});
const marketKb=m=>({inline_keyboard:[
 [{text:'⚡ MARKET NOW',callback_data:`v11:scan:${m}`},{text:'🔥 TOP 3',callback_data:`v11:top3:${m}`}],
 [{text:'📈 LONG SETUPS',callback_data:`v11:side:${m}:LONG`},{text:'📉 SHORT SETUPS',callback_data:`v11:side:${m}:SHORT`}],
 [{text:'🏆 MARKET WR',callback_data:`v11:marketwr:${m}`},{text:'📚 LIVE',callback_data:`v11:market:${m}`}],
 [{text:'🔎 TRA SYMBOL',callback_data:'v11:symbol:help'},{text:'📋 SYMBOLS',callback_data:`v11:symbol:list:${m}`}],
 [{text:'👀 WATCH',callback_data:`v11:watch:${m}`},{text:'🧠 5 AI • MARKET',callback_data:'v11:ai:hunt'}],
 [{text:'🏠 HUB',callback_data:'menu'}]
]});

function fmt(v){if(v===null||v===undefined||v==='')return '—';const n=Number(v);if(!Number.isFinite(n)||n<=0)return '—';const a=Math.abs(n);let s=a>=1000?n.toFixed(2):a>=1?n.toFixed(4):a>=.01?n.toFixed(5):a>=.0001?n.toFixed(6):n.toPrecision(5);return s.replace(/(\.\d*?[1-9])0+$|\.0+$/,'$1');}
const pnlPct=(side,entry,price)=>{const e=Number(entry),p=Number(price);if(!(e>0)||!(p>0))return null;return (String(side).toUpperCase()==='SHORT'?(e-p):(p-e))/e*100;};
const pnlText=p=>p==null?'⚪ —':p>=0?`🟢 +${p.toFixed(2)}%`:`🔴 ${p.toFixed(2)}%`;
const line=x=>{const c=x.candidate||{},p=pnlPct(c.side,c.entry,x.currentPrice),q=x.currentQuote||{},age=Number.isFinite(Number(q.quoteAgeSec))?`${Math.round(Number(q.quoteAgeSec))}s`:'—';return `${c.side==='LONG'?'🟢':'🔴'} ${x.symbol} ${c.side==='LONG'?'BUY':'SELL'} | ${pnlText(p)}\nE ${fmt(c.entry)} • N ${fmt(x.currentPrice)}\nSL ${fmt(c.sl)} • TP ${fmt(c.tp)} • RR ${Number(c.rr||0).toFixed(1)}\n📡 ${q.source||'NO LIVE'} ${age} • AI CUT ${x.lastAiCutVotes||0}/5`;};
async function send(env,id,text,markup=kb()){return telegramApiRequest(env,'sendMessage',{chat_id:id||env.TELEGRAM_CHAT_ID,text,reply_markup:markup,disable_web_page_preview:true});}
function aiText(x){if(!x)return '—';if(x.status!=='OK')return x.status||'—';const r=x.review||{};return `${r.direction||'WAIT'} ${Number(r.confidence||0)}%`;}
function hunterText(r){if(!r?.best)return `🧠 5 AI MARKET\n⚪ Chưa có entry đủ chuẩn\n${r?.reason||r?.status||'NO_CANDIDATE'}`;const x=r.best,c=x.candidate||{},ai=x.ai||{};return [`🧠 5 AI MARKET`,`${c.side==='LONG'?'🟢 BUY':'🔴 SELL'} ${x.symbol} • ${String(x.market||'').toUpperCase()}`,`E ${fmt(c.entry)} • SL ${fmt(c.sl)} • TP ${fmt(c.tp)} • RR ${Number(c.rr||0).toFixed(1)}`,`AI ${Number(x.consensusCount||0)}/5 ${x.consensus?'✅':'⚠️'} • C:${aiText(ai.claude)} • X:${aiText(ai.codex)}`,`D:${aiText(ai.deepseek)} • Q:${aiText(ai.qwen)} • O:${aiText(ai.openrouter)}`].join('\n');}
function watchRows(f,market){const rows=(f||[]).filter(x=>x.status==='WATCH'&&(!market||x.market===market)),seen=new Set(),out=[];for(const x of rows){const k=`${x.market}:${x.symbol}:${x.reason}`;if(seen.has(k))continue;seen.add(k);out.push(x);if(out.length>=8)break;}return out;}
function watchText(rows){if(!rows.length)return '👀 WATCH • Trống';return ['👀 WATCH',...rows.map(x=>`${String(x.market||'').toUpperCase()} • ${x.symbol} • ${x.reason||'WAIT'}`)].join('\n');}
function marketWrText(h,market){const closed=(h||[]).filter(x=>x.outcome==='WIN'||x.outcome==='LOSS'),wins=closed.filter(x=>x.outcome==='WIN').length,loss=closed.length-wins,cuts=(h||[]).filter(x=>x.outcome==='CUT').length,expired=(h||[]).filter(x=>x.outcome==='EXPIRED').length,wr=closed.length?wins/closed.length*100:null;return `🏆 ${String(market||'V11').toUpperCase()} WR\n✅ ${wins} • ❌ ${loss} • ✂️ ${cuts} • ⌛ ${expired}\n🎯 ${wr==null?'—':wr.toFixed(1)+'%'} • mẫu ${closed.length}`;}
function topRows(a,n=3,side=null){return (a||[]).filter(x=>!side||String(x.candidate?.side||'').toUpperCase()===side).sort((x,y)=>Number(y.candidate?.qualityScore||0)-Number(x.candidate?.qualityScore||0)).slice(0,n);}
async function enrichLive(rows,env){return Promise.all((rows||[]).map(async x=>{try{const q=await getLiveQuote(x.symbol,x.market,env);return {...x,currentPrice:Number(q.price),currentQuote:q};}catch{return {...x,currentPrice:null,currentQuote:{fresh:false,source:'NO_FRESH_LIVE'}};}}));}

function symbolsText(market=null){const grouped={crypto:[],forex:[],metal:[],index:[]};for(const [s,p] of Object.entries(INSTRUMENT_PROFILES))if(grouped[p.market])grouped[p.market].push(s);for(const k of Object.keys(grouped))grouped[k].sort();const groups=market?[market]:['crypto','forex','metal','index'];return ['📋 V11 SYMBOLS',...groups.flatMap(m=>[`\n${m.toUpperCase()} (${grouped[m].length})`,grouped[m].join(' • ')])].join('\n');}
function symbolHelp(){return `🔎 TRA & ĐÁNH GIÁ SYMBOL\nGửi theo mẫu:\n/symbol AAVE\n/symbol BTCUSDT\n/symbol EURUSD\n/symbol XAUUSD\n/symbol NAS100\n\nBot sẽ lấy LIVE quote mới + phân tích V11 riêng symbol đó.`;}
const finite=v=>{const n=Number(v);return Number.isFinite(n)?n:null;};
async function analyzeOneSymbol(rawSymbol,env){const input=String(rawSymbol||'').trim().toUpperCase().replace(/[^A-Z0-9/_:-]/g,'');if(!input)return '⚠️ Thiếu symbol.';const canonical=canonicalInstrument(input),profile=instrumentStrategy(canonical);if(!profile.known)return `⚠️ ${input}: chưa có profile V11.\nBấm 📋 ALL SYMBOLS để xem danh sách hỗ trợ.`;const market=profile.market;let q=null,qErr=null;try{q=await getLiveQuote(input,market,env);}catch(e){qErr=String(e?.message||e);}let a=null;try{const r=await baseEngine.fetch(new Request(`https://v11.internal/analyze?symbol=${encodeURIComponent(input)}`),env);a=await r.json().catch(()=>null);}catch{}const status=String(a?.status||a?.canonical?.status||'UNAVAILABLE').toUpperCase(),side=String(a?.side||a?.planned?.side||a?.canonical?.side||'WAIT').toUpperCase(),quality=finite(a?.qualityScore??a?.quality??a?.score),planned=a?.planned||{},entry=finite(q?.price??a?.entry??planned.entry),sl=finite(a?.sl??planned.sl),tp=finite(a?.tp2??a?.tp??a?.tp1??planned.tp2??planned.tp??planned.tp1),rr=finite(a?.targetRR??planned.targetRR)??((entry&&sl&&tp)?Math.abs(tp-entry)/Math.abs(entry-sl):null),setup=a?.entryPolicy?.trigger?.mode||planned.entryStyle||a?.setup||profile.families?.[0]||'—';let verdict='👀 WATCH';if(['MARKET','MARKET_SIGNAL'].includes(status)&&['LONG','SHORT'].includes(side)&&q?.fresh===true)verdict='⚡ MARKET READY';else if(['LIMIT','LIMIT_PLAN'].includes(status))verdict='🎯 LIMIT';else if(status==='REJECTED')verdict='⛔ REJECT';const age=q&&Number.isFinite(Number(q.quoteAgeSec))?`${Math.round(Number(q.quoteAgeSec))}s`:'—',cross=q?.crossSourceCount?` • x${q.crossSourceCount}`:'';return [`🔎 ${input} → ${canonical} • ${market.toUpperCase()}`,`${verdict} • ${side} • Q ${quality==null?'—':Math.round(quality)+'/100'}`,`LIVE ${fmt(q?.price)} • ${q?.source||'NO LIVE'} ${age}${cross}`,`E ${fmt(entry)} • SL ${fmt(sl)} • TP ${fmt(tp)} • RR ${rr==null?'—':rr.toFixed(1)}`,`🧭 ${setup}`,qErr?`⚠️ Quote: ${qErr.slice(0,120)}`:null].filter(Boolean).join('\n');}

async function textFor(env,kind,market,side=null){const a=await getV11Accepted(env,market),h=await getV11History(env,market),f=await getV11Funnel(env);if(kind==='live'||kind==='official'){const picked=market?a.slice(0,5):a.slice(0,20),rows=await enrichLive(picked,env);return [`🔥 ${market?market.toUpperCase():'V11'} LIVE • ${rows.length}${market?'/5':''}`,...rows.map(line)].join('\n\n');}if(kind==='top3'){const rows=await enrichLive(topRows(a,3),env);return rows.length?[`🔥 TOP 3 ${String(market||'V11').toUpperCase()}`,...rows.map(line)].join('\n\n'):`🔥 TOP 3 ${String(market||'V11').toUpperCase()} • Trống`;}if(kind==='side'){const rows=await enrichLive(topRows(a,5,side),env);return rows.length?[`${side==='LONG'?'📈 LONG':'📉 SHORT'} ${String(market||'V11').toUpperCase()}`,...rows.map(line)].join('\n\n'):`${side} ${String(market||'V11').toUpperCase()} • Trống`;}if(kind==='marketwr')return marketWrText(h,market);if(kind==='watch')return watchText(watchRows(f,market));if(kind==='history')return [`🕘 HISTORY • ${h.length}`,...h.slice(0,12).map(x=>`${x.outcome==='WIN'?'✅':x.outcome==='LOSS'?'❌':x.outcome==='CUT'?'✂️':'⌛'} ${x.symbol} • ${x.outcome} • ${fmt(x.closePrice)}`)].join('\n');if(kind==='stats'){const wins=h.filter(x=>x.outcome==='WIN').length,loss=h.filter(x=>x.outcome==='LOSS').length,cuts=h.filter(x=>x.outcome==='CUT').length,n=wins+loss;return `📈 V11 STATS\n✅ ${wins} • ❌ ${loss} • ✂️ ${cuts}\nWR ${n?(wins/n*100).toFixed(1):'—'}%`;}if(kind==='system')return `📊 SYSTEM\n✅ Signal V11\n✅ LIVE Quote Router\n✅ Crypto pair resolver BASE→USDT\n✅ Multi-source crypto quote consensus\n✅ 5 AI Gateway\n📌 Max 5 MARKET / thị trường\n✂️ CUT cần ≥4/5 AI`;return `🤖 TRADING HUB V11\n🧠 5 AI • 📡 LIVE quote\n🔎 Tra riêng mọi symbol có profile\n📌 Max 5 MARKET / thị trường\n✂️ AI CUT 4/5\nChọn thị trường.`;}
function auth(u,env){const got=String(u?.callback_query?.from?.id??u?.message?.from?.id??''),want=String(env.TELEGRAM_ALLOWED_USER_ID||env.TELEGRAM_CHAT_ID||'');return !want||got===want;}
function marketParam(url){const m=String(url.searchParams.get('market')||'');return ['crypto','forex','metal','index'].includes(m)?m:null;}
async function v11Api(url,env){const market=marketParam(url);if(url.pathname==='/v11/signals'||url.pathname==='/v11/live'){const base=(await getV11Accepted(env,market)).slice(0,market?5:20),rows=await enrichLive(base,env);return json({ok:true,version:'V11',market,signals:rows});}if(url.pathname==='/v11/watch'){const f=await getV11Funnel(env);return json({ok:true,version:'V11',market,watch:watchRows(f,market)});}if(url.pathname==='/v11/history')return json({ok:true,version:'V11',market,history:await getV11History(env,market)});if(url.pathname==='/v11/funnel')return json({ok:true,version:'V11',funnel:await getV11Funnel(env)});if(url.pathname==='/v11/symbol'&&url.searchParams.get('symbol'))return json({ok:true,text:await analyzeOneSymbol(url.searchParams.get('symbol'),env)});return null;}
async function diagnosticAnalyze(url,env){const symbol=String(url.searchParams.get('symbol')||'').trim().toUpperCase();if(!symbol)return json({ok:false,error:'SYMBOL_REQUIRED'},400);const r=await baseEngine.fetch(new Request(`https://v11.internal/analyze?symbol=${encodeURIComponent(symbol)}`),env);let payload=null;try{payload=await r.json()}catch{payload={error:'NON_JSON_INTERNAL_ANALYSIS'}}return json({ok:r.ok,version:'V11',symbol,httpStatus:r.status,payload},r.ok?200:502);}

export default {async fetch(req,env,ctx){const url=new URL(req.url);if(url.pathname==='/v11/status')return json({ok:true,version:'V11',telegram:true,maxOpenPerMarket:5,reevaluationCut:true,aiCutConsensusRequired:4,canonicalLiveQuoteRouter:true,symbolLookup:true});const api=await v11Api(url,env);if(api)return api;if(url.pathname.startsWith('/v11/')){if(url.pathname==='/v11/diagnostic/analyze'&&req.method==='GET')return diagnosticAnalyze(url,env);if(url.pathname==='/v11/scan'&&req.method==='POST'){const body=await req.json().catch(()=>({})),market=String(body.market||'');if(!['crypto','forex','metal','index'].includes(market))return json({ok:false,error:'INVALID_MARKET'},400);return json(await scanMarketV11(env,market));}if(url.pathname==='/v11/ai-hunt'&&req.method==='POST')return json(await huntWholeMarket(env));return json({ok:false,error:'V11_ENDPOINT_NOT_FOUND'},404);}if(url.pathname==='/telegram/webhook'&&req.method==='POST'){let u;try{u=await req.clone().json()}catch{return json({ok:false},400)}if(!auth(u,env))return json({ok:false},403);const cb=String(u?.callback_query?.data||''),msg=String(u?.message?.text||'').trim(),id=u?.callback_query?.message?.chat?.id??u?.message?.chat?.id??env.TELEGRAM_CHAT_ID;if(/^\/(symbol|s)\s+/i.test(msg)){const sym=msg.replace(/^\/(symbol|s)\s+/i,'').trim();await send(env,id,await analyzeOneSymbol(sym,env),kb());return json({ok:true,owner:'V11',symbol:sym});}if(msg==='/symbols'){await send(env,id,symbolsText(),kb());return json({ok:true,owner:'V11'});}if(cb.startsWith('v11:')||msg==='/start'||msg==='/v11'||cb==='menu'){if(cb==='v11:symbol:help'){await send(env,id,symbolHelp(),kb());return json({ok:true,owner:'V11'});}if(cb.startsWith('v11:symbol:list')){const p=cb.split(':'),m=p[3]||null;await send(env,id,symbolsText(m),m?marketKb(m):kb());return json({ok:true,owner:'V11'});}if(cb==='v11:ai:hunt'){await send(env,id,'⏳ 5 AI đang quét…');const r=await huntWholeMarket(env);await send(env,id,hunterText(r));return json({ok:true,owner:'V11',hunter:r.status});}let kind='menu',market=null,side=null;if(cb.startsWith('v11:scan:')){market=cb.split(':')[2];await send(env,id,`⏳ Quét ${market.toUpperCase()}…`,marketKb(market));const r=await scanMarketV11(env,market);await send(env,id,`🔍 ${market.toUpperCase()} • +${r.notified||0} signal • WATCH ${r.watch||0} • reject ${r.rejected||0}`,marketKb(market));return json({ok:true,owner:'V11',scan:r});}if(cb.startsWith('v11:marketwr:')){market=cb.split(':')[2];kind='marketwr';}else if(cb.startsWith('v11:top3:')){market=cb.split(':')[2];kind='top3';}else if(cb.startsWith('v11:side:')){const p=cb.split(':');market=p[2];side=p[3];kind='side';}else if(cb.startsWith('v11:watch:')){market=cb.split(':')[2];kind='watch';}else if(cb.startsWith('v11:market:')){market=cb.split(':')[2];kind='live';}else if(cb==='v11:live')kind='live';else if(cb==='v11:watch')kind='watch';else if(cb==='v11:history')kind='history';else if(cb==='v11:stats')kind='stats';else if(cb==='v11:system')kind='system';await send(env,id,await textFor(env,kind,market,side),market?marketKb(market):kb());return json({ok:true,owner:'V11'});}return json({ok:false,owner:'V11',error:'LEGACY_TELEGRAM_DISABLED'},404);}if(url.pathname==='/'||url.pathname==='/hub')return json({ok:true,version:'V11',service:'SIGNAL_V11',binanceAutoSeparate:true});return json({ok:false,version:'V11',error:'LEGACY_PUBLIC_ENDPOINT_DISABLED'},410);},async scheduled(event,env,ctx){return scheduledNativeV11(event,env,ctx);}};
