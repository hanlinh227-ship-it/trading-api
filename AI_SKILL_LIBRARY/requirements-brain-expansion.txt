# Optional Brain Expansion adapter dependencies.
#
# NOT installed by AI_SKILL_LIBRARY/requirements.txt and NOT installed by the
# brain validator CI. The stable brain must keep working with none of these
# present, and every adapter stays off until it verifies.
#
# Auto-activation is AUTO_ACTIVATE_WHEN_VERIFIED: installing a line below makes
# that adapter's `dependency_available` condition pass, but the adapter still
# enables only if every other condition it requires also passes. Installing a
# package is permission to try, never an instruction to enable.
#
# Versions are pinned to the exact audited upstream refs recorded in
# AI_SKILL_LIBRARY/v4/integrations/brain_expansion_adapters.yaml, and the
# activation runtime (v4/tools/activate_brain_expansion.py) re-probes them.
#
# None of these enables billing. The sandbox tests are deterministic and make no
# LLM-provider calls, so installing them costs nothing to run.

# --- runtime-verified: these reached state=enabled under a real probe sweep ---
baml-py==0.226.2
ragas==0.4.3
deepeval==4.1.8

# --- installable, but the adapter still fails closed without a credential -----
# The MIT SDK only. The langfuse/langfuse server monorepo is open-core (MIT Expat
# core plus a commercial ee/ licence) and must never be an executable dependency.
# With the SDK present but LANGFUSE_PUBLIC_KEY / LANGFUSE_SECRET_KEY unset, the
# adapter reports credential_present=FAIL and stays off. That is the intended
# behaviour, not a defect.
langfuse==4.15.4

# --- runtime-verified elsewhere: browser-use has an interpreter of its own -----
# browser-use additionally requires Playwright plus a Chromium binary, and
# resolving that beside a distribution-managed interpreter has to replace
# packages the system package manager owns (PyJWT among them). That is why it is
# deliberately absent here rather than force-installed.
#
# Its runtime now exists: a dedicated virtualenv built from
# AI_SKILL_LIBRARY/requirements-brain-expansion-browser.txt, where the adapter
# reaches state=enabled with all eight of its gates PASS against a real Chromium
# process. Installing it into *this* file's environment is what breaks; nothing
# here needs to change for browser_use to be live.
