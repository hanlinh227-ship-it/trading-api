# GITHUB_BRAIN_V3 — Compatibility Redirect

`GITHUB_BRAIN_V3` is retained only as a compatibility activation alias.

The current canonical brain authority is `GITHUB_BRAIN_V4`.

1. Read `AI_SKILL_LIBRARY/checkpoint.json`.
2. Follow `checkpoint_path` and `release_pointer_path`.
3. Use `AI_SKILL_LIBRARY/GITHUB_BRAIN_V4.md` and the validated V4 Stable release.

V3 does not define independent current authority and cannot override V4, project authority, or runtime state.
