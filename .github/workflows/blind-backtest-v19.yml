name: Blind Crypto Backtest V19
on:
  push:
    paths:
      - 'scripts/blind_backtest_crypto_v19.py'
      - '.github/workflows/blind-backtest-v19.yml'
  workflow_dispatch:
jobs:
  v19:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/blind_backtest_crypto_v19.py
      - run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/blind_backtest_v19.json
          git commit -m "Run V19 fresh blind" || exit 0
          git pull --rebase origin main || true
          git push || true
