name: Public Source Probe
on:
  push:
    paths:
      - '.github/workflows/public-source-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe endpoints
        run: |
          set +e
          for u in \
            'https://stooq.com/q/d/l/?s=eurusd&d1=20260101&d2=20260815&i=d' \
            'https://query1.finance.yahoo.com/v8/finance/chart/EURUSD=X?interval=1d&period1=1767225600&period2=1786752000' \
            'https://query1.finance.yahoo.com/v8/finance/chart/EURUSD=X?interval=1h&period1=1767225600&period2=1786752000' \
            'https://raw.githubusercontent.com/ejtraderLabs/historical-data/main/EURUSD/EURUSDh4.csv' \
            'https://raw.githubusercontent.com/komo135/forex-historical-data/main/EURUSD/EURUSDh4.csv' \
            'https://freeserv.dukascopy.com/2.0/?path=api/instrumentList&fields=id,name' \
            'https://freeserv.dukascopy.com/2.0/?path=api/historicalPrices&instrument=1&timeFrame=1hour&count=10' \
            'https://api.binance.com/api/v3/klines?symbol=BTCUSDT&interval=4h&limit=2' \
            'https://www.okx.com/api/v5/market/candles?instId=BTC-USDT&bar=4H&limit=2' \
            'https://api.bybit.com/v5/market/kline?category=spot&symbol=BTCUSDT&interval=240&limit=2'
          do
            echo "URL=$u"
            curl -L -sS --max-time 8 -o /tmp/out -w 'HTTP=%{http_code} TIME=%{time_total} SIZE=%{size_download}\n' "$u"
            head -c 220 /tmp/out; echo
            echo '---TAIL---'; tail -c 220 /tmp/out; echo
          done
