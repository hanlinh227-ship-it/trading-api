name: Meme Alpha v3.12 Activate New Listing Radar (Retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: [self-hosted, linux]
    steps:
      - run: echo 'V312_RETIRED_USE_V313_SAFE_ACTIVATION'
