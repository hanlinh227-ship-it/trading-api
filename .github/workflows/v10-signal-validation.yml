name: Legacy V10 Signal Validation (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: V10 gate retired
        run: |
          echo 'V10 signal validation is intentionally retired.'
          echo 'Signal V11 is the sole public signal authority.'
          echo 'Use V11 Signal Validation for current production source.'
