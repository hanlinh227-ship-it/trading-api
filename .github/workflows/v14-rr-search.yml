name: V14 RR Search
on:
  push:
    paths:
      - 'scripts/v14_rr_search.py'
      - '.github/workflows/v14-rr-search.yml'
  workflow_dispatch:
jobs:
  rr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/v14_rr_search.py
      - run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/v14_rr_search.json
          git commit -m "Run V14 dynamic RR search" || exit 0
          git pull --rebase origin main || true
          git push || true
