name: Offline Crypto V35 Fast
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/offline_crypto_v35_fast.py'
      - '.github/workflows/offline-crypto-v35-fast.yml'
permissions:
  contents: read
jobs:
  offline:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run focused corrected Crypto V35
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python scripts/offline_crypto_v35_fast.py
