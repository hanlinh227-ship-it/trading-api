name: Meme Alpha v3.60 Inspect Safe Deploy
on:
  workflow_dispatch:
  push:
    paths:
      - 'ops/meme-alpha/v360-inspect-safe-deploy.sh'
      - '.github/workflows/meme-alpha-v360-inspect-safe-deploy.yml'
jobs:
  inspect:
    runs-on: [self-hosted, linux]
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v360-inspect-safe-deploy.sh
