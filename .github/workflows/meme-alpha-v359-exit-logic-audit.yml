name: Meme Alpha v3.59 Exit Logic Audit
on:
  workflow_dispatch:
  push:
    paths:
      - 'ops/meme-alpha/v359-exit-logic-audit.sh'
      - '.github/workflows/meme-alpha-v359-exit-logic-audit.yml'
jobs:
  audit:
    runs-on: [self-hosted, linux]
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v5
      - name: Inspect live exit logic
        run: bash ops/meme-alpha/v359-exit-logic-audit.sh
