name: Legacy V77 Validation (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: V77 gate retired
        run: |
          echo 'V77 validation is intentionally retired.'
          echo 'Signal V11 is the sole public signal authority.'
          echo 'Use V11 Signal Validation plus Deploy Cloudflare Signal V11.'
