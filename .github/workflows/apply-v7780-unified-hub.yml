name: Archived V77.8 migration helper

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V77.8.0 is already canonical in cloudflare-worker/index.js. This migration workflow is archived and must not modify production.'
