name: Blind Backtest Forex F4 All Pairs Dynamic

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/blind_backtest_forex_f4_allpairs_dynamic.py"
      - ".github/workflows/blind-backtest-forex-f4-allpairs-dynamic.yml"

permissions:
  contents: write

concurrency:
  group: twelvedata-api
  cancel-in-progress: false

jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run Forex F4 all-pair dynamic blind test
        env:
          TWELVEDATA_API_KEY: ${{ secrets.TWELVEDATA_API_KEY }}
        run: |
          test -n "$TWELVEDATA_API_KEY" || (echo "TWELVEDATA_API_KEY missing" && exit 1)
          python scripts/blind_backtest_forex_f4_allpairs_dynamic.py
      - name: Commit result
        run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/blind_backtest_forex_f4_allpairs_dynamic.json
          git commit -m "Run Forex F4 all-pair dynamic blind validation" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
