name: V77.16.9 Hyro-Only Prop Shell (SUPERSEDED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded notice
        run: |
          echo "V77.16.9 was promoted directly without GitHub Actions."
          echo "Canonical shell: cloudflare-worker/index.js"
          echo "Preserved engine: cloudflare-worker/engine-v77168.js"
          echo "Do NOT run the old migration script against the wrapper."
          echo "See docs/checkpoints/V77169_HYRO_DIRECT_PROMOTION.md"
