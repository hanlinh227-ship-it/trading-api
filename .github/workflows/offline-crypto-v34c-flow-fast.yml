name: Offline Crypto V34C Flow Fast
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/offline_crypto_v34c_flow_fast.py'
      - '.github/workflows/offline-crypto-v34c-flow-fast.yml'
permissions:
  contents: read
jobs:
  offline:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run focused Crypto V34C flow refinement
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python scripts/offline_crypto_v34c_flow_fast.py
