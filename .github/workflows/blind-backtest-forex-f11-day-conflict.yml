name: Blind Backtest Forex F11 Day Conflict

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/blind_backtest_forex_f11_day_conflict.py"
      - ".github/workflows/blind-backtest-forex-f11-day-conflict.yml"

permissions:
  contents: write

concurrency:
  group: twelvedata-api
  cancel-in-progress: false

jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Cool down Twelve Data quota
        run: sleep 70
      - name: Run F11 day-conflict blind comparator
        env:
          TWELVEDATA_API_KEY: ${{ secrets.TWELVEDATA_API_KEY }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          test -n "$TWELVEDATA_API_KEY" || (echo "TWELVEDATA_API_KEY missing" && exit 1)
          python scripts/blind_backtest_forex_f11_day_conflict.py
      - name: Commit result
        run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/blind_backtest_forex_f11_day_conflict.json
          git commit -m "Run F11 day-conflict blind comparator" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
