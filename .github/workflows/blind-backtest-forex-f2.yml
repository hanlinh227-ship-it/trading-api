name: Blind Backtest Forex F2

on:
  workflow_dispatch:
  push:
    paths:
      - "scripts/blind_backtest_forex_f2.py"
      - ".github/workflows/blind-backtest-forex-f2.yml"

permissions:
  contents: write

concurrency:
  group: twelvedata-api
  cancel-in-progress: false

jobs:
  backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run Forex F2 blind test
        env:
          TWELVEDATA_API_KEY: ${{ secrets.TWELVEDATA_API_KEY }}
        run: |
          test -n "$TWELVEDATA_API_KEY" || (echo "TWELVEDATA_API_KEY missing" && exit 1)
          python scripts/blind_backtest_forex_f2.py
      - name: Commit result
        run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/blind_backtest_forex_f2.json
          git commit -m "Run Forex F2 blind validation" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
