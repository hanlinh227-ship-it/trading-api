name: Meme Alpha v2.9 Exit Detail Audit
on:
  push:
    paths:
      - 'ops/meme-alpha/v296-exit-detail-audit.sh'
      - '.github/workflows/meme-alpha-v296-exit-detail-audit.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v296-exit-detail-audit.sh
