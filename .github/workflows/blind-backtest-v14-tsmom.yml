name: Blind Crypto Backtest V14 TSMOM
on:
  push:
    paths:
      - 'scripts/blind_backtest_crypto_v14_tsmom.py'
      - '.github/workflows/blind-backtest-v14-tsmom.yml'
  workflow_dispatch:
jobs:
  blind:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/blind_backtest_crypto_v14_tsmom.py
      - run: |
          git config user.name "blind-backtest-bot"
          git config user.email "blind-backtest-bot@users.noreply.github.com"
          git add data/blind_backtest_v14_tsmom.json
          git commit -m "Run V14 TSMOM fresh blind" || exit 0
          git pull --rebase origin main || true
          git push || true
