name: Meme Alpha v2.9 Live Executor Header Audit
on:
  push:
    paths:
      - 'ops/meme-alpha/v297-live-executor-header-audit.sh'
      - '.github/workflows/meme-alpha-v297-live-executor-header-audit.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v297-live-executor-header-audit.sh
