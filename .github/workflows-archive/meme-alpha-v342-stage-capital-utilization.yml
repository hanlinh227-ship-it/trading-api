name: Meme Alpha v3.42 Stage Capital Utilization
on:
  push:
    branches: [main]
    paths:
      - 'ops/meme-alpha/v342-stage-capital-utilization.sh'
      - '.github/workflows/meme-alpha-v342-stage-capital-utilization.yml'
  workflow_dispatch:
jobs:
  stage:
    runs-on: [self-hosted, linux]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v342-stage-capital-utilization.sh
