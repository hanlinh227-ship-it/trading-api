name: Meme Alpha v2.9 Path Trace
on:
  push:
    paths:
      - 'ops/meme-alpha/v293-path-trace.sh'
      - '.github/workflows/meme-alpha-v293-path-trace.yml'
  workflow_dispatch:

jobs:
  trace:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v293-path-trace.sh
