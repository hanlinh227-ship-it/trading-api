name: Meme Alpha v2.16B Scanner Tail Audit
on:
  push:
    paths:
      - 'ops/meme-alpha/v216b-scanner-tail-audit.sh'
      - '.github/workflows/meme-alpha-v216b-scanner-tail-audit.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v216b-scanner-tail-audit.sh
