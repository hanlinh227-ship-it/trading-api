name: Meme Alpha v2.1.1 Inspect Security Deadlock
on:
  push:
    paths:
      - 'ops/meme-alpha/v211-inspect-security-deadlock.sh'
      - '.github/workflows/meme-alpha-v211-inspect-security-deadlock.yml'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: [self-hosted, trading-vps]
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - run: bash ops/meme-alpha/v211-inspect-security-deadlock.sh
