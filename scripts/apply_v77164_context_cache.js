const fs=require('fs');
const p='cloudflare-worker/index.js';let s=fs.readFileSync(p,'utf8');
function must(a,b,l){if(!s.includes(a))throw new Error('Missing '+l);s=s.replace(a,b);}
if(!s.includes('version: "V77.16.3"'))throw new Error('Expected V77.16.3');
s=s.replaceAll('V77.16.3','V77.16.4');
s=s.replace('Trading V77.16.4 Stable Direct-Symbol Quality Hub','Trading V77.16.4 Context-Cached Stable Quality Hub');
must('    futureContract: "v7714:future_contract",\n','    futureContract: "v7714:future_contract",\n    contextPrefix: "v77164:context:",\n','context key');
const cacheFns=`\nasync function saveMarketContext(group,data,env){try{await env.TRADING_STATE?.put(CONFIG.keys.contextPrefix+group,JSON.stringify({savedAt:Date.now(),data}),{expirationTtl:300});}catch{}}\nasync function loadMarketContext(group,env,maxAgeMs=180000){try{const v=await env.TRADING_STATE?.get(CONFIG.keys.contextPrefix+group,"json");if(v?.savedAt&&Date.now()-Number(v.savedAt)<=maxAgeMs&&v.data)return v.data;}catch{}return null;}\n`;
must('async function cryptoDerivativesContext(symbol){',cacheFns+'async function cryptoDerivativesContext(symbol){','context cache helpers');
must('  let h1Map=new Map();try{h1Map=await tdBatchCandles(symbols,"1h",env,60);}catch(e){return {requested:symbols.length,rows,errors:symbols.map(symbol=>({symbol,reason:"H1_BATCH_UNAVAILABLE",error:e?.message||String(e)})),h1Map};}const fx=group==="forex"?forexStrengthMap(h1Map):{};let metalMoves={};if(group==="metal")for(const sym of symbols)metalMoves[sym]=changeFromCandles(h1Map.get(sym)||[]);','  let h1Map=new Map();try{h1Map=await tdBatchCandles(symbols,"1h",env,60);}catch(e){return {requested:symbols.length,rows,errors:symbols.map(symbol=>({symbol,reason:"H1_BATCH_UNAVAILABLE",error:e?.message||String(e)})),h1Map};}const fx=group==="forex"?forexStrengthMap(h1Map):{};let metalMoves={};if(group==="metal")for(const sym of symbols)metalMoves[sym]=changeFromCandles(h1Map.get(sym)||[]);if(group==="forex")await saveMarketContext("forex",{strength:fx},env);else if(group==="metal")await saveMarketContext("metal",{moves:metalMoves},env);','save broad context');
const oldMirror='function mirrorFutureAnalysis(a,target){if(!a)return null;const k=symbolKnowledge(target),out={...a,symbol:target,market:"future",source:(a.source||"Massive Futures")+" • leader mirror",knowledge:k?{canonicalSymbol:k.canonicalSymbol,allowedModes:k.allowedModes,riskATR:k.riskATR,calibration:k.calibration}:a.knowledge,executionLeader:a.symbol,basisReference:true};const fs=futureSizing(target,out.planned||out);if(fs)out.microSizing=fs;return out;}';
const newMirror='function mirrorFutureAnalysis(a,target){if(!a)return null;const k=symbolKnowledge(target),out={...a,symbol:target,market:"future",source:(a.source||"Massive Futures")+" • leader mirror",knowledge:k?{canonicalSymbol:k.canonicalSymbol,allowedModes:k.allowedModes,riskATR:k.riskATR,calibration:k.calibration}:a.knowledge,executionLeader:a.symbol,basisReference:true};const fs=futureSizing(target,out.planned||out);if(fs)out.microSizing=fs;if(fs&&!fs.tradable&&["MARKET","LIMIT","MARKET_PLAN","LIMIT_PLAN"].includes(out.status)){out.rejectedPlan=out.planned||{entry:out.entry,sl:out.sl,tp1:out.tp1,tp2:out.tp2,targetRR:out.targetRR};out.planned=null;out.status="WATCH";out.action="WATCH";out.reason="FUTURE_RISK_LIMIT_REQUIRED";out.canonicalStage=out.reason;out.executionReady=false;}return out;}';
must(oldMirror,newMirror,'mirror risk sanitize');
const oldFx='  if(type==="forex"){const h1=await tdBatchCandles(FOREX,"1h",env,60),fx=forexStrengthMap(h1),base=s.slice(0,3),quote=s.slice(3),d=(fx[base]||0)-(fx[quote]||0),context={baseStrength:fx[base]||0,quoteStrength:fx[quote]||0,strengthDiff:d,score:Math.min(10,5+Math.abs(d)*4)},extra=await Promise.all(["5min","15min","4h","1day"].map(i=>tdBatchCandles([s],i,env,CONFIG.candleOutputSize))),candles=[extra[0].get(s)||[],extra[1].get(s)||[],h1.get(s)||[],extra[2].get(s)||[],extra[3].get(s)||[]];let q=null;try{q=await tdQuote(s,env);}catch{}const a=await deepAnalyze(s,env,candles,q,"Twelve Data direct + 28-pair strength",context);if(q)a.analysisQuote=q;return a;}';
const newFx='  if(type==="forex"){let cached=await loadMarketContext("forex",env),fx=cached?.strength||null,h1;if(fx){h1=await tdBatchCandles([s],"1h",env,60);}else{h1=await tdBatchCandles(FOREX,"1h",env,60);fx=forexStrengthMap(h1);await saveMarketContext("forex",{strength:fx},env);}const base=s.slice(0,3),quote=s.slice(3),d=(fx[base]||0)-(fx[quote]||0),context={baseStrength:fx[base]||0,quoteStrength:fx[quote]||0,strengthDiff:d,score:Math.min(10,5+Math.abs(d)*4),contextCached:!!cached},extra=await Promise.all(["5min","15min","4h","1day"].map(i=>tdBatchCandles([s],i,env,CONFIG.candleOutputSize))),candles=[extra[0].get(s)||[],extra[1].get(s)||[],h1.get(s)||[],extra[2].get(s)||[],extra[3].get(s)||[]];let q=null;try{q=await tdQuote(s,env);}catch{}const a=await deepAnalyze(s,env,candles,q,"Twelve Data direct + cached 28-pair strength",context);if(q)a.analysisQuote=q;return a;}';
must(oldFx,newFx,'direct forex cache');
const oldMetal='  if(type==="metal"){const h1=await tdBatchCandles(METALS,"1h",env,60),moves={};for(const x of METALS)moves[x]=changeFromCandles(h1.get(x)||[]);const other=s==="XAUUSD"?"XAGUSD":"XAUUSD",rel=(moves[s]||0)-(moves[other]||0),context={relativeStrength:rel,benchmark:other,score:Math.min(10,5+Math.abs(rel)*3)},extra=await Promise.all(["5min","15min","4h","1day"].map(i=>tdBatchCandles([s],i,env,CONFIG.candleOutputSize))),candles=[extra[0].get(s)||[],extra[1].get(s)||[],h1.get(s)||[],extra[2].get(s)||[],extra[3].get(s)||[]];let q=null;try{q=await tdQuote(s,env);}catch{}const a=await deepAnalyze(s,env,candles,q,"Twelve Data direct + metal relative",context);if(q)a.analysisQuote=q;return a;}';
const newMetal='  if(type==="metal"){let cached=await loadMarketContext("metal",env),moves=cached?.moves||null,h1;if(moves){h1=await tdBatchCandles([s],"1h",env,60);}else{h1=await tdBatchCandles(METALS,"1h",env,60);moves={};for(const x of METALS)moves[x]=changeFromCandles(h1.get(x)||[]);await saveMarketContext("metal",{moves},env);}const other=s==="XAUUSD"?"XAGUSD":"XAUUSD",rel=(moves[s]||0)-(moves[other]||0),context={relativeStrength:rel,benchmark:other,score:Math.min(10,5+Math.abs(rel)*3),contextCached:!!cached},extra=await Promise.all(["5min","15min","4h","1day"].map(i=>tdBatchCandles([s],i,env,CONFIG.candleOutputSize))),candles=[extra[0].get(s)||[],extra[1].get(s)||[],h1.get(s)||[],extra[2].get(s)||[],extra[3].get(s)||[]];let q=null;try{q=await tdQuote(s,env);}catch{}const a=await deepAnalyze(s,env,candles,q,"Twelve Data direct + cached metal relative",context);if(q)a.analysisQuote=q;return a;}';
must(oldMetal,newMetal,'direct metal cache');
must('TIMEFRAME_DATA_REQUIRED:"Thiếu timeframe",ANALYSIS_DATA_UNAVAILABLE:"Thiếu dữ liệu"','TIMEFRAME_DATA_REQUIRED:"Thiếu timeframe",ANALYSIS_DATA_UNAVAILABLE:"Thiếu dữ liệu",FUTURE_RISK_LIMIT_REQUIRED:"SL cấu trúc vượt giới hạn risk micro",MASSIVE_RATE_OR_DATA_UNAVAILABLE:"Massive đang giới hạn hoặc thiếu dữ liệu"','reason labels');
fs.writeFileSync(p,s);console.log('Applied V77.16.4 context cache and mirror safeguards');
